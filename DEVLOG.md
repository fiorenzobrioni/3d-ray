# 📋 DEVLOG — 3D-Ray

Roadmap, lavori in corso, bug noti, storico cicli.

> Stati: `✅ Fatto` · `🔧 In corso` · `⬜ Da fare`

---

## 📌 Note rapide

---

## 🗺️ Roadmap

### Fase 0 — Fondamenta ✅

Path tracer multi-bounce, parallel render, BVH SAH, camera DOF + multi-camera, primitive base + trasformazioni, materiali Lambertian/Metal/Dielectric, luci Point/Directional/Spot/Area + NEE, Russian Roulette adattiva, stratified sampling, ACES + gamma + firefly guard, YAML loader, output PNG/JPEG/BMP, CI smoke test.

### Fase 1 — Visivo ✅

| # | Feature |
|---|---------|
| 1 | Emissive Material (diventa GeometryLight per NEE) |
| 2 | Gradient Sky (zenith / horizon / ground + sun disc) |
| 3 | Image Textures (PNG/JPG/BMP/GIF/TIFF/WebP, bilinear, tiling) |
| 4 | IBL / HDRI (Radiance .hdr, CDF 2D marginal+conditional, rotazione Y) |
| 5 | Normal Mapping (TBN + Gram-Schmidt, OpenGL/DirectX) |

### Fase 2 — Materiali & geometria ✅

| # | Feature |
|---|---------|
| 6 | Disney BSDF / PBR (vedi sotto) |
| 7 | OBJ Mesh Loader (smooth normals, UV, TBN, BVH interno) |
| 8 | Torus (quartica Ferrari, UV toroidale, NEE, CSG, Transform) |
| 9 | Mix Material (selezione stocastica scatter, blend deterministico NEE, mask qualsiasi texture) |
| 10 | Sphere Light (solid-angle sampling PBRT §6.2.3, 2-10× più efficiente di GeometryLight) |
| 11 | Scene Graph / Groups (transform ereditate, template+instance, import YAML con merge) |

**Disney BSDF** include: lobi diffuse / GGX / clearcoat / transmission, Kulla-Conty multi-scattering (LUT 32×32), GGX anisotropico (VNDF), Beer-Lambert via medium-switch, parametri Disney 2015 (`thin_walled`, `diff_trans`, `flatness`, `subsurface_color`), clearcoat stile Arnold (`coat_ior`, `coat_roughness`, `coat_normal`), Charlie sheen (Estevez-Kulla 2017), thin-film iridescence (Belcour-Barla 2017). MIS-correct (`Sample`/`Evaluate`/`Pdf` consistenti, furnace + reciprocity test).

### Fase 3 — Sampling avanzato 🔧

| # | Feature | Stato |
|---|---------|-------|
| 12 | Importance Sampling (GGX su Metal/Disney, env via CDF 2D, cosine-weighted diffuse) | ✅ |
| 13 | Multi-Importance Sampling (tutti i materiali + phase function, balance/power heuristic) | ✅ |
| 14 | Adaptive Sampling | ⬜ (dopo #15) |
| 15 | Tile-based Rendering | ⬜ |
| 16 | Denoiser (bilateral/NLMeans guidato da normal/albedo/depth) | ⬜ (dopo #15) |
| 17 | HDR Output (PFM/EXR pre-tone-mapping) | ⬜ |
| +  | Sobol + Owen Scrambling sampler (`--sampler sobol`, default attivo) | ✅ |

### Fase 4 — Cinematografici 🔧

| # | Feature | Stato |
|---|---------|-------|
| 18 | Motion Blur | ⬜ |
| 19 | Volumetric Rendering | 🔧 Stage 1 + 1.5 ✅ |
| 20 | Subsurface Scattering | ⬜ |
| 21 | CSG (union/intersection/subtraction, all-hits, normali corrette) | ✅ |
| 22 | Instancing (geometria condivisa, override material/seed per-istanza) | ✅ |
| +  | Extrusion primitive (linear/catmull_rom/bezier + twist + taper + caps) | ✅ |
| +  | Transparent shadow rays (vetro Fresnel-tinted in NEE — Strada 1) | ✅ |

**Volumetrics Stage 1+1.5**: medium globale opt-in (`world.medium`), output bit-identico se assente. `IMedium`: Homogeneous (Beer-Lambert + free-path), HeightFog (densità esponenziale closed-form), HeterogeneousProcedural (Perlin fBm, delta+ratio tracking), Grid (`.vol` o inline, slab clip + trilinear + delta tracking). `IPhaseFunction`: Isotropic, HG, Rayleigh, Double-HG (Nubis), Schlick (fast-HG). Stage 2 (deferred): EmissiveMedium, MediumInterface per-entity, SSS random-walk, OpenVDB nativo, spectral tracking — tutti richiedono modifiche ad `IMedium` / `HitRecord` / `Renderer.TraceRay`.

### Fase 5 — Frontiera ⬜

| # | Feature |
|---|---------|
| 23 | Bidirectional Path Tracing (dopo #13) |
| 24 | Spectral Rendering (lunghezze d'onda → dispersione prismatica) |
| 25 | Displacement Mapping (height map runtime tessellation, dopo #7) |
| 26 | GPU Acceleration (CUDA/Vulkan, progetto separato) |

### Dipendenze chiave

```
#3 Image Tex ─► #5 Normal Map, #9 Mix Material
#6 Disney   ─► #20 SSS
#7 OBJ      ─► #22 Instancing, #25 Displacement
#11 Scene G ─► #22 Instancing
#12 IS      ─► #13 MIS ─► #23 BDPT
#15 Tiles   ─► #14 Adaptive, #16 Denoiser
```

---

## 🌟 Roadmap caustiche

Strategia incrementale per le caustiche, in ordine di costo crescente. Strada 1 è la baseline ora attiva; Strada 2 è il prossimo target sensato; Strada 3 è opzionale e architetturalmente invasiva.

| Strada | Cosa risolve | Effort | Stato |
|--------|--------------|--------|-------|
| 1. Transparent shadow rays | Ombra dura del vetro → soft Fresnel-tinted (Arnold/Cycles default) | 1-2 giorni | ✅ |
| 2. MNEE (Manifold Next Event Estimation) | Caustiche focalizzate single-bounce attraverso una specular (lenti, bicchieri d'acqua, finestre). Cycles 3.2 "Shadow Caustics" | 2-3 settimane | ⬜ |
| 3. SPPM / VCM (photon mapping) | Caustiche multi-bounce, dispersive, indipendenti da differenziabilità della geometria. RenderMan PxrVCM | 6-10 settimane (SPPM) / 3-5 mesi (VCM) | ⬜ |

**Strada 1 — Transparent shadow rays ✅** Implementata. Lo shadow ray attraversa superfici trasmissive accumulando `(1 − Fresnel) · tint` per canale + Beer-Lambert `exp(−σ_a · d)` sul segmento interno fra entrata e uscita. Helper `Geometry/ShadowRay.Transmittance` con cap di 8 traversate; override `IMaterial.ShadowTransmittance` + `IMaterial.ShadowAbsorption` su `Dielectric` (no σ_a) / `DisneyBsdf` (entrambi quando `spec_trans > 0`) / `MixMaterial` (blend di entrambi).
**Limiti residui**: nessuna rifrazione dello shadow ray (no caustiche di lente in NEE); `roughness > 0` con `spec_trans > 0` (frosted glass) ignorata — lo shadow ray va dritto come vetro liscio. Per entrambi servono MNEE (Strada 2) o SPPM/VCM (Strada 3).

**Strada 2 — MNEE ⬜** Walker Newton-Raphson sulla manifold della superficie speculare; cerca un cammino `x → y_spec → light` che soddisfi Snell. Single-vertex robusto (Hanika/Droske/Manzi 2015, riferimento Cycles/Mitsuba).
**Pro**: unbiased, niente seconda passata, zero memoria extra, MIS-friendly, 10-100× più veloce del PT puro per caustiche.
**Contro**: limitato a 1 (forse 2) interfacce in serie; richiede normali differenziabili (`∂n/∂u`, `∂n/∂v`); fallisce su mesh hard-edge e bordi CSG (skip senza bias).
**Lavoro stimato**: nuovo `IManifoldGeometry` con derivate parametriche su `Sphere`/`Cylinder`/`Cone`/`Torus`/`Disk`/`Quad`/`SmoothTriangle`; `Rendering/ManifoldWalker.cs` (~400 righe); hook in `ComputeDirectLighting`; opt-in YAML `caustic_caster`/`caustic_receiver` per non sprecare campioni dove non serve. Test analitico (sfera-vetro vs piano, soluzione closed-form) come `BvhEquivalenceTests` per il BVH.

**Strada 3 — SPPM/VCM ⬜** Pass 1 emette fotoni dalle luci e li deposita su superfici diffuse in un kd-tree; pass 2 fa density estimation durante il rendering. SPPM raffina il raggio progressivamente per convergenza unbiased; VCM combina BDPT vertex connections + photon merging via MIS (Georgiev 2012, gold standard).
**Pro**: caustiche multi-bounce, dispersive (con spectral), indipendenti dalla geometria; beneficio collaterale su indirect diffuse (final gathering accelerato).
**Contro**: due pass in serie (cambia l'orchestrazione del Renderer); 50-500 MB di memoria per il photon map; dispersione richiede upgrade spettrale a monte (RGB-only sbaglia il prisma); separazione delta-vs-diffuse nel cammino fotone è sottile.
**Lavoro stimato**: `Acceleration/PhotonMap.cs` (~600 righe, kd-tree con range query); `Rendering/PhotonEmitter.cs` (~400 righe); modifica profonda di `Renderer` (fase build prima del shading); CLI `--photons N --photon-radius r --sppm-iterations n`; profilo Caustic.

**Decisione corrente**: Strada 1 sufficiente per i casi d'uso showcase quotidiani; Strada 2 è il candidato per quando si vorranno caustiche pulite delle lenti in `csg-showcase.yaml`; Strada 3 resta opzione lunga-roadmap (utile solo se servono caustiche multi-bounce/dispersive).

---

## ✅ TODO

- [x] ~~Valutare se coerente: aggiungere la possibilita tramite una nuova proprieta per le luci visibili nella scena (tipo ad es. le luci `sphere`) per renderle visibili o meno dal punto di vista della camera, oltre che per l'illuminazione.~~ Fatto: `visible_to_camera` (Arnold/Cycles "camera" / Cycles "Ray Visibility → Camera") su `lights:` (sphere/area) e `entities:` (qualunque oggetto, utile per emissive panels). Vedi voce 2026-05 nello storico.
- [x] ~~Valutare se coerente: aggiungere proprietà `focal_pos: [x, y, z]` (alternativa a `focal_dist`) per la `camera` per indicare la posizione del fuoco al posto della distanza. Per semplicita' in certe situazioni potrebbe essere comodo indicare direttamente la posizione del fuoco.~~ Fatto: `focal_pos` su `CameraData`, distanza calcolata come proiezione del vettore camera→punto sull'asse ottico (Arnold "Focus Object"/Cycles "Focal Object"/RenderMan). Vedi voce 2026-05 nello storico.
- [x] ~~Texture più realistiche (devono essere come quelle dei ray tracer professionali tipo Arnold, Cycles, Renderman, ecc.) di quelle attuali per legno, marmo, noise e tutti le texture gestite finora. Aggiungi eventuali texture ora assenti ma presenti in ray tracer come Arnold, Cycles, Renderman, ecc.. Implementazione pro a livello di Arnold, Cycles, Renderman, ecc.. Senza compromessi. Aggiungi nuova scena showcase e aggiorna anche reference scene IT e EN e i tutorial IT e EN.~~ Fatto: upgrade pro di noise/marble/wood (octaves/lacunarity/gain/distortion + assi/sharpness configurabili) e nuove texture `voronoi`, `brick`, `gradient`. Vedi voce 2026-05 nello storico.
- [x] ~~**Stack completo "surface displacement" livello Arnold/RenderMan/Cycles** — deformazioni superficiali visibili con parità feature ai render pro. Da sviluppare come unico ciclo a step incrementali, ciascuno utile da solo: (1) **Bump map scalare da `ITexture` qualunque** (procedurali + immagini): nuovo canale `bump_map: { texture: …, strength: …, scale: … }` sui materiali, perturbazione della normale geometrica via differenze finite di luminanza in tangent-space (TBN già esistente da #5 normal mapping); funziona su tutte le primitive senza modifiche al BVH — sblocca subito le texture pro (#27) come dettagli superficiali. (2) **Mesh subdivision** (Loop su tri, Catmull-Clark opzionale su quad) sul loader OBJ con `subdivision_iterations` e/o `subdivision_pixel_error` (adattivo screen-space). (3) **Scalar displacement vero** sulla mesh subdivisa (`v += h(u,v) · n_smooth`), con `displacement_bound` per gonfiare gli AABB del BVH così i micro-poligoni spostati non scappano dal box originale — questo è il #25 della Fase 5 e fornisce le silhouette modificate, non solo lo shading. (4) **Vector displacement** (texture RGB → offset XYZ) per overhangs e crinkles, riusando l'infrastruttura di (3). (5) **Combinazione bump + displacement** (autobump-like di Arnold: il displacement gestisce la macro-silhouette, il bump residuo i dettagli sub-pixel) e priorità di applicazione coerente con Disney BSDF (`coat_normal_map` indipendente già esistente, base normal/bump/displacement compongono il `n_shading` finale). Vincolo architetturale: il displacement vero resta limitato alle mesh — sphere/torus/cylinder/ecc. supportano solo bump (stessa scelta di Arnold/Cycles). Doc + showcase + tutorial EN/IT per ogni step.~~ Fatto: tutti e cinque gli step completati. Step 5 (autobump + composizione canonica `normal_map → bump_map → autobump`) implementato nel ciclo 2026-05; vedi voce dedicata nello storico.

- [x] ~~**Texturing "VFX production-grade": parità completa con Arnold/RenderMan/Cycles oltre la matematica core.**~~ ✅ **ROADMAP COMPLETA** (2026-05). Tutti i 7 step completati: (1) anti-aliasing analitico con ray differentials, (2) color ramp multi-stop, (3) smooth Voronoi (IQ log-sum-exp), (4) Musgrave HeteroTerrain + HybridMultifractal, (5) Marble/Wood studio-quality (secondary wave, anisotropy, knots), (6) F3/F4 + Position output Voronoi, (7) CoordinateTexture node. Vedi voci dedicate nello Storico cicli. **Contesto originale** (preservato per riferimento storico): I primitivi di noise (Perlin, fBm, ridged, billow, Worley, marble, wood, brick, gradient) usano già le equazioni canoniche dei testi di riferimento (Perlin 1985/2002, Ebert-Musgrave-Peachey-Perlin "Texturing & Modeling", Worley 1996, IQ domain warp), e per un singolo sample il risultato è equivalente. Mancano però gli strati di infrastruttura e tooling che separano un raytracer "decente con texture credibili" da uno "pronto per VFX dove le texture devono reggere zoom 4K, movimento camera, e workflow di lookdev". Da sviluppare come unico ciclo a step incrementali ordinati per impatto visivo decrescente, ciascuno utile da solo:

  (1) **Anti-aliasing analitico con filter footprint (ray differentials).** ✅ Completato 2026-05 (branch `claude/analytic-antialiasing-filter-36oaF`, voce in Storico cicli). È il fix più urgente: oggi le procedurali sono **point-sampled**, a distanza producono moiré/shimmer dove Arnold/RM mostrerebbero sfumatura. Estendere `Ray` con ray differentials `(∂P/∂x, ∂P/∂y, ∂D/∂x, ∂D/∂y)` in screen-space come in PBRT §10.1, propagati attraverso `Hit()` di ogni primitiva (`sphere`/`cylinder`/`torus`/`cone`/`quad`/`triangle`/`disk`/`annulus`/`capsule`/`lathe`/`extrusion`/`CSG` — formula chiusa per ciascuna, le derivate sono già implicite nelle parametrizzazioni esistenti) e attraverso `Transform` (Jacobiana = matrice inversa-trasposta). Estendere `ITexture` con overload `Value(u, v, p, seed, FilterFootprint footprint)` con default che fa pass-through alla versione point-sampled corrente (back-compat). Implementazioni filtered: **Perlin/fBm** via clamp ottave a `λ = ⌊log₂(1/maxAxis(footprint))⌋` (Heidrich-Slusallek 1998 "Improved Perlin Noise" §4) — sopra Nyquist le ottave alte vengono droppate analiticamente; **Worley** via supersampling adattivo 4-16 jitter samples in footprint (`PxrVoronoise` fa lo stesso); **ImageTexture** via mipmap pyramid generata in ctor + EWA filtering (Heckbert 1989) per anisotropia corretta a basso angolo. CLI flag `--texture-filtering on|off|auto` (default `auto`). Test: rendering `textures-pro-showcase.yaml` a 4K, 16 spp → confronto rumore vs baseline 256 spp; risultato atteso: stesso aspetto. Bench: `TextureFilteringBench` per costo per-sample.

  (2) **Color ramp multi-stop.** ✅ Completato 2026-05 (branch `claude/color-ramp-multi-stop-ZlQ5y`, voce in Storico cicli). Nuova classe `ColorRamp` esposta come blocco YAML opzionale `color_ramp: [...]` al posto del semplice `colors:` su qualunque texture procedurale. Lista di stop `{ position: float ∈ [0,1], color: [r,g,b], interp: "linear" | "smoothstep" | "constant" | "ease" }`. Applicato in `ITexture.Value` dopo il calcolo del valore scalare di noise (sostituisce il `Vector3.Lerp(colorA, colorB, t)` finale di noise/marble/wood/voronoi/gradient — brick ha tre colori specifici e resta indipendente o usa ramp 3-stop). Esempio YAML:
  ```yaml
  texture:
    type: "marble"
    color_ramp:
      - { position: 0.00, color: [0.05, 0.05, 0.07], interp: "linear" }
      - { position: 0.45, color: [0.95, 0.93, 0.88], interp: "smoothstep" }
      - { position: 0.55, color: [0.95, 0.93, 0.88], interp: "linear" }
      - { position: 1.00, color: [0.05, 0.05, 0.07], interp: "linear" }
  ```
  `colors:` resta come scorciatoia equivalente a un ramp a 2 stop (back-compat totale). Sblocca: marmo Statuario con vena dorata/nera/grigia, wood con sapwood/heartwood/knot 3-color, gradient artistici complessi.

  (3) ~~**Smooth Voronoi.** Aggiungere `smoothness ∈ [0,1]` a `VoronoiTexture`: quando > 0 sostituisce `min()` su F1 con soft-min `-log(Σ exp(-k·d_i)) / k` (k = 20/smoothness, IQ "Smooth Voronoi"); F2-F1 con smoothness > 0 diventa "smooth crackle" (bordi morbidi, niente alias a step). Utile per cuoio levigato, ciottoli arrotondati, pelle di rettile più realistica. Showcase: tre sfere `hard / smooth=0.3 / smooth=0.7`.~~ ✅ Completato 2026-05 (branch `claude/smooth-voronoi-texturing-nqCt1`, voce in Storico cicli).

  (4) ~~**Musgrave multifractal completo.** Aggiungere a `Perlin` i metodi `HeteroTerrain(p, octaves, lacunarity, H, offset)` e `HybridMultifractal(p, octaves, lacunarity, H, offset)` da Musgrave "Texturing & Modeling" §16. Parametro `H` (fractal increment, controlla roughness vs altitudine), `offset` (sea-level/threshold). Esposti in `NoiseTexture` come `noise_type: "hetero_terrain"` e `"hybrid_multifractal"` con i parametri YAML `fractal_increment` (H) e `fractal_offset`. Sblocca terreni proceduralmente erosi (terra a quote diverse con roughness diversa) e pattern roccia stratificati irraggiungibili con fBm puro.~~ ✅ Completato 2026-05 (branch `claude/smooth-voronoi-texturing-nqCt1`, voce in Storico cicli).

  (5) ~~**Marble e Wood "studio quality".** Upgrade dei due shader esistenti senza breaking change:
  - **Marble**: blocco YAML opzionale `secondary_wave: { axis: [...], frequency: ..., strength: ... }` per stratificare una seconda sinusoide ortogonale alla principale → marmi a doppia direzione di venatura (Statuario, Calacatta, Arabescato). La somma `sin(wave1) + 0.5 · sin(wave2)` produce un campo non più rigidamente unidirezionale. Usa il color ramp del punto (2) per vena/base/sotto-tinta a 3+ stop.
  - **Wood**: separazione di `grain_scale` (alta freq, dettaglio fibra interna agli anelli) e `figure_scale` (bassa freq, ondulazione tavola tipo curly maple), entrambi pesati indipendentemente da `grain_strength` / `figure_strength`. Aggiunta di `radial_anisotropy: float` che stretchera il noise lungo l'asse radiale vs tangenziale (rovere quartato = anisotropia alta, piano-sawn = bassa). Sapwood/heartwood gradient via color ramp 3-stop dal punto (2). Optional `knot_density` per spawn casuale di nodi via Voronoi piccolo-scala mascherato.~~ ✅ Completato 2026-05 (branch `claude/smooth-voronoi-texturing-nqCt1`, voce in Storico cicli).

  (6) ~~**F3/F4 e output Voronoi estesi.** Aggiungere F3, F4 a `WorleyNoise.Evaluate` (mantiene complessità O(27) sulle 27 celle, costo marginale poiché già si scansionano), e nuovi `OutputMode.F3`, `F4`, `F3MinusF1`, `Position` (posizione XYZ del feature più vicino come RGB — utile per shading per-cella o per modulare un'altra texture). Cycles e Houdini espongono F3/F4 di default; in pratica raro ma necessario per cellulare gerarchico e shading custom.~~ ✅ Completato 2026-05 (branch `claude/voronoi-texturing-vfx-gSqWR`, voce in Storico cicli).

  (7) ~~**Coordinate texture node.** Nuova `CoordinateTexture` che ritorna `(p.x, p.y, p.z)` o `(u, v, 0)` come RGB, con `mode: "world" | "object" | "uv" | "generated"` e trasformazione (offset/rotation/scale) standard. Analogo al "Texture Coordinate" node di Cycles. Utile per debug visivo dei UV/coord-spaces e per pilotare altre texture (texture-driven mix masks già supportato ma quello è specifico per il mix material; questo è generico). Bassa priorità — QoL per artisti.~~ ✅ Completato 2026-05 (branch `claude/voronoi-texturing-vfx-gSqWR`, voce in Storico cicli). **🎉 ROADMAP "VFX production-grade textures" CHIUSA — tutti i 7 step completati.**

  Per ogni step controlla attentamente il render della scena di prova e se vedi che non è corretto/realistico, correggi subito.
  Per ogni step: aggiornare `docs/reference/scene-reference.md` + `riferimento-scene.md` + `docs/tutorial/{en,it}/03-materials.md`, aggiungere showcase dedicato o sezione in `textures-pro-showcase.yaml`, regression test dove ha senso (es. `RayDifferentialTests` per (1) — proiezione di footprint noto attraverso transform e verifica numerica; `ColorRampTests` per (2) — interpolazioni su stop noti; `SmoothVoronoiTests` per (3) — continuità verificata su griglia). Validazione finale: rendering `textures-pro-showcase.yaml` a 1920×1080 e 4K con 64 spp, confronto con baseline pre-cambio sulla stessa scena (`renders/textures-pro-showcase.png` attuale è il punto di partenza). Voce DEVLOG storica a fine ciclo che elenca tutti gli step completati e i file toccati. Vincoli architetturali: back-compat totale (le scene YAML esistenti devono renderizzare identiche se non usano le nuove feature); tutti i 187+ test esistenti devono continuare a passare; nessun degrado di performance > 5% sui benchmark esistenti (`dotnet run -c Release --project src/RayTracer.Benchmarks -- --filter '*'`).
- [ ] Fai una review dei materiali in libraries/materials e aggiorna quelli che possono giovare, per diventare più realistici, delle features "surface displacement". Poi aggiungi anche altre librerie professionali di materiali basate su "surface displacement": ad esempio pelli, cementi, sassi, marmi e altri di tipo poroso. Guarda i materiali (di tipo poroso o che comunque beneficiano di "surface displacement") che hanno altri renderer pro (Arnlod, Cycles, Renderman, ecc) e crea tu delle nuove categorie estremamente professionali.
- [x] ~~Aggiornare README.md in root con le nuove feature aggiunte qui sopra negli step "surface displacement" e "texturing "VFX production-grade" che non sono ancora presenti.~~ Fatto 2026-05 (stesso branch `claude/voronoi-texturing-vfx-gSqWR`). Aggiornata la sezione "Texture" con i nuovi knob studio-quality di marble (secondary_wave) e wood (figure/radial_anisotropy/knot_density), i canali estesi Voronoi (F3/F4/F3−F1/Position) + smoothness, le varianti Musgrave (hetero_terrain/hybrid_multifractal) e il mipmap+EWA su `image`. Aggiunte due nuove voci: **Color Ramp multi-stop** e **Coordinate** texture. Aggiunta nuova sotto-sezione **Texture Filtering (Anti-Aliasing Analitico)** con la sintesi delle tre famiglie di filtering (Perlin clamp ottave, Voronoi supersampling adattivo, Image mipmap+EWA) e il flag CLI `--texture-filtering`. La sezione "Surface Displacement Stack" era già completa dal ciclo dedicato (bump, subdivision, scalar/vector displacement, autobump) — nessun aggiornamento necessario lì.
- [ ] Refactoring: spostare `Seed` da `IHittable` a un'interfaccia dedicata (es. `ISeeded`); nodi strutturali (BvhNode, Transform) non hanno bisogno di seed.
- [ ] Review completa dei tutorial (`tutorial/`): correttezza vs codice, omissioni, grammatica, esempi, indici.
- [ ] Spezzare `SceneLoader.cs`.

---

## 🐛 Bug noti

| # | Descrizione | Severità | Stato |
|---|-------------|----------|-------|
| 2 | RNG globale seedato da `Environment.TickCount` (`MathUtils.cs:12`): due render della stessa scena producono rumore stocastico diverso. A spp ≥ 64 il rumore si media; a spp basso le differenze sono visibili. Non riguarda i pattern procedurali (bug #1 risolto). Architettura proposta: sampler per-pixel deterministico via hash `(pixelX, pixelY, sampleIndex)`. Da affrontare con #15 Tile-based. | 🟠 Media | ⬜ |

---

## 📚 Riferimenti tecnici

### RNG globale e determinismo (riferimento bug #2)

`MathUtils.cs:12-17` espone un RNG thread-local seedato da `Environment.TickCount`. Usato per BSDF / NEE / DoF / RR / jitter, quindi ogni esecuzione parte da uno stato diverso → pixel diversi tra render della stessa scena.

**Quando dà fastidio**: visual regression testing, A/B di parametri (diff = parametro + rumore), animazioni (flickering temporale tra frame), debug ("perché questo pixel?").

**Architettura proposta**:
1. `Sampler` per-pixel che incapsula lo stato RNG, seedato da hash deterministico `(pixelX, pixelY, sampleIndex)` (PCG / xoshiro / splittable RNG).
2. Propagare il `Sampler` lungo `TraceRay` / `Scatter` / `EvaluateDirect` / `SampleLight` come parametro esplicito (rimuove `MathUtils.Rng` singleton).
3. CLI `--render-seed N` (default 0 → render identici; override per variazione intenzionale tra frame).

**Impatti**: output bit-identico, smoke test verificabile contro baseline (anche hash dell'immagine), rimozione del counter atomico globale, miglior cache locality con #15 Tiles. L'immagine "finale" della scena cambierà rispetto ai render attuali.

**Dipendenza**: meglio dopo (o insieme a) #15 Tile-based, perché il tile è il granulo naturale per il seeding per-pixel.

---

## 🗓️ Storico cicli

- **2026-05 — Ciclo HeightField + TerrainGen heightfield-mode.** Branch `claude/upgrade-terrain-generation-xt0nb`. Sostituzione completa del primitivo terreno e del tool che lo genera. **Engine**: nuovo `IHittable HeightField` (`src/RayTracer/Geometry/HeightField.cs`) — superficie continua `y = h(x, z) · height_scale` su un rettangolo XZ, intersecata da una **min/max mipmap** quadtree (Tevs/Ihrke/Seidel 2008, l'algoritmo del plugin `heightfield.cpp` di Mitsuba 0.x). La heightmap arriva da PNG-16 grayscale (`HeightmapLoader` via `Image.Load<L16>`) o viene sintetizzata al caricamento da una qualsiasi `ITexture` campionata su una griglia `resolution × resolution`. Hit a 3 fasi: slab AABB → traversal della piramide near→far → 12 step di bisezione sulla patch bilineare della cella foglia (`f(t) = ray.Y(t) − sampleHeight(ray.X(t), ray.Z(t))`). Normali da differenze finite centrali con `ε = mezza cella` (consistenti con la patch bilineare effettivamente intersecata). Sistema **strata altitudine/pendenza** per shadeggiare la stessa heightfield con materiali multipli (sabbia/erba/roccia/neve): ogni band ha plateau-con-fade su `altNorm` e `slopeDeg`, vince la band col peso massimo (v1 = winner-takes-all; lerp tra band adiacenti è un follow-up). Piano d'acqua opzionale (`sea_level` + `sea_material`) clippato al footprint, visibile solo dove il terreno sotto è effettivamente sotto il livello. Schema YAML completo (`bounds`, `max_height`, `height_scale`, `heightmap_path`/`height_texture`, `resolution`, `sea_level`, `sea_material`, `strata[]`, `material`) in `docs/reference/scene-reference.md` §7.13; algoritmo dettagliato in `docs/technical/heightfield.md`. **Test**: 8 nuovi xUnit (`HeightFieldTests.cs`) coprono flat/ramp/miss/sea-sopra-terra/terra-sopra-mare/strata-alto/strata-basso/AABB. **Tool**: `TerrainGen` riscritto in-place — pipeline 6→5 step (la tassellazione scompare). Riusati `Heightmap2D`, `NoiseStack`, `TerrainShaper`, `Erosion/*`, `Hydrology/*` (~570 righe di codice intatto); rimossi `Output/ObjWriter.cs`, `Splatting/MeshBuilder.cs`, `Splatting/StylePostprocessor.cs` (~440 righe morte). Nuovo `Output/PngHeightmapWriter.cs` (PNG-16 via SixLabors), `YamlEmitter` riscritto per emettere un singolo `type: heightfield` con band strata, `StratumClassifier` ripurposed da classificatore per-triangolo a builder della tabella band. **Rimozione completa del flag `--style realistic|minecraft|lowpoly`** end-to-end: era specifico della mesh tessellation e non ha analogo su heightfield. **Cleanup**: 21 file legacy rimossi (`alps-test`, `minecraft-test`, `pianura-spring`, `smoke` — relativi `.obj`, `.yaml` template e preview scenes). `scenes/libraries/terrain/` ora ricomincia vuota e viene ripopolata dai nuovi `<name>-height.png` + `<name>.yaml`. CI render-matrix guadagna due voci commentate (`heightfield-procedural-test`, `heightfield-strata-test`) per smoke render opzionali. Tutta la test suite (406/406) verde.

- **2026-05 — Voronoi `Random` output + mesh-proxy showcase migration.** Branch `claude/fix-concrete-materials-ig6Jb`. Il canale `OutputMode.Cell` del nodo Voronoi era documentato come "hash RGB per cella" (parità Cycles "Color"), ma 53 materiali della libreria lo usavano con una `colors:` palette muted aspettandosi un colore stocastico vincolato — risultato: arcobaleno saturo su sassi, scaglie, ciottoli, asfalti consumati, ecc. Refactor VFX-grade in due mosse: (a) `Cell` torna al suo significato originale (raw RGB hash, Cycles `Color`); (b) nuovo `OutputMode.Random` esposto via alias YAML `random` / `rnd` / `id` — scalare deterministico per cella (Murmur3 finalizer su `WorleyNoise.CellScalar`) mappato attraverso `colors:` / `color_ramp:` esattamente come gli output distanza. Equivalente 1:1 al "Random" output di Cycles 3.0+ (Arnold `cell_noise` scalar, RenderMan `PxrVoronoise` per-cell ID). Migrazione meccanica di tutte le 53 occorrenze `output: "cell"` → `output: "random"` su 14 librerie (`biological`, `ceramics`, `concretes`, `emissives`, `fabrics`, `grounds`, `leathers`, `metals`, `minerals-gems`, `organics`, `plastics`, `stones`, `synthetics`, `weathering`); l'unica usage genuino di Cell come stochastic-RGB-ID (`textures-pro-showcase.yaml`) resta intatto. **Showcase migration parallela**: 14 showcase "fila di sfere demo per libreria materiali" (concretes, glass-features, leathers, plasters, minerals-gems, woods-studio-v2, biological, industrial-coatings, liquids, marbles-studio-v2, synthetics, weathered-surfaces, weathering-overlay, wet-surfaces) sostituiscono le `type: "sphere"` analitiche con `type: "mesh"` su `subdivision-icosahedron.obj` + Loop subdivision adattiva (`subdivision_pixel_error: 6.0`, `subdivision_max_iterations: 5`); così i materiali con `displacement:` esibiscono finalmente il profilo geometrico nel showcase invece di emettere un warning silenzioso. Nessun cambio a `sphere-showcase.yaml` (già su icosaedro). Doc bilingue completa: `docs/reference/scene-reference.md` + `riferimento-scene.md` ottengono la sezione "Per-cell stochastic ID — cell vs random vs position" e una raccomandazione esplicita "Substitute analytical primitives with mesh proxies for displacement" con tabella proxy stock; `docs/tutorial/{en,it}/03-materials.md` aggiungono la sezione didattica "Mesh-only — and how to fix it on spheres" con ricetta sphere→icosahedron, scelta tra subdivision adattiva e fissa, e note sui CSG/lathe/torus. 2 nuovi test di regressione (`VoronoiTexture_Random_RespectsPaletteEndpoints`, `VoronoiTexture_Random_SamplesColorRampWhenAttached`, `VoronoiTexture_Random_IsPerCellConstant`, `VoronoiTexture_Random_DecorrelatedFromCell`, `VoronoiTexture_Cell_IsRawRgbHash_IgnoresPalette`) — 398/398 test passano. Engine refactor zero-compromessi: parità semantica 1:1 con Cycles 3.0+ (Color e Random come canali distinti), nessun breaking change per scene che usavano Cell come ID-RGB grezzo.

- **2026-05 — Displacement model: entity → material (Cycles/RenderMan parity).** Branch `claude/displacement-material-behavior-PSmFG`. Il blocco `displacement` è stato spostato dall'entity al material: un singolo material displaced ora guida ogni mesh che lo referenzia, senza duplicazione per-entity — parità con il socket "Material Output → Displacement" di Cycles e con la dichiarazione `PxrDisplace` nella shader network di RenderMan (Arnold resta l'eccezione fra i renderer pro col `disp_map` sulla shape). **Breaking change**: i campi `displacement:` e `displacement_bound:` sull'entity ora rendono un `InvalidOperationException` esplicito che indirizza l'autore alla migrazione (testato: `MaterialDisplacementTests.LegacyEntityDisplacementYaml_RaisesLoadError`). Le 4 scene showcase pre-esistenti (scalar, vector, bump-displacement-combo, sphere) sono state riscritte per portare il blocco displacement sul material, generando varianti dedicate (`porcellana_perlin`, `porcellana_voronoi`, `rock_basalt_voronoi`, `rock_basalt_vector_ridged`, ecc.) quando lo stesso material di base serviva con displacement diversi. **Architettura — Materials layer**: nuovo `MaterialDisplacement` (gerarchia sealed `LeafDisplacement` / `MixDisplacement`); slot `Displacement => null` come default interface property su `IMaterial` accanto a `NormalMap`/`BumpMap`; ciascun concrete material (Lambertian / Metal / Dielectric / DisneyBsdf / Emissive / MixMaterial) espone `Displacement { get; set; }`. `MixMaterial.EvaluateBlendFactor` reso `internal` per permettere a `DisplacementEngine` di calcolare il fattore di blend per-vertex al mesh-load time, prima di qualunque HitRecord. **MixDisplacement vector-blend** (Cycles "Mix Shader → Displacement"): un `MixMaterial` con `displacement: { blend_with_mask: true }` vector-blenda le offset per-vertex dei due child usando la stessa mask del BSDF mix; il nuovo `DisplacementEngine.ApplyMix` valuta `t(u,v,p)` al vertex e fa `(1-t)·offsetA + t·offsetB` con basis indipendenti per ciascun child (tangent/object/smooth-normal accumulati in object space). Risultato C0-continuo lungo le cuciture material. **Loader & ObjLoader**: `BuildDisplacementOptions(EntityData)` rinominato `BuildLeafDisplacement(MaterialData)`, spostato nel pass-1 del two-pass material-build di `SceneLoader`. Pass-2 (mix materials) costruisce il `MixDisplacement` quando il YAML del Mix ha `displacement: { blend_with_mask: true }`, con warning + fallback se uno dei due child non ha displacement. `ObjLoader.Load` ora accetta `MaterialDisplacement?` invece di `DisplacementOptions`, dispatcha su `LeafDisplacement`/`MixDisplacement`, e rispetta `entity.displacement_enabled: false` per il bypass per-istanza. Warning di load per entità non-mesh (sphere analitica, CSG, group, instance) che referenziano un material displaced. **Tri-state Cycles `displacement_method`**: `both` (default, geometric + autobump), `displacement` (solo geometric, no autobump), `bump_only` (solo bump, skip geometric pass). Modellato sul socket "Displacement" del Material Output di Cycles. **`displacement_enabled` per-entity**: un singolo bool sulla mesh entity sopprime il displacement del material risolto per quella sola istanza, mantenendo il material condiviso per tutti gli altri usi. Utile per LOD/proxy. **Autobump composition per Mix**: nuovo `MixBumpMapTexture : IBumpMap` (introdotto un'interface `IBumpMap` per uniformare `BumpMapTexture` e `MixBumpMapTexture`); l'autobump di un `MixDisplacement` blenda le normali tangent-space dei due child autobump via lo stesso mask del BSDF mix, normalizzato. `Mesh.AutoBump` e `HitRecord.AutoBump` cambiano tipo da `BumpMapTexture?` a `IBumpMap?`; `Renderer.ApplyBumpMap` rilegge da `IBumpMap`. Composition order nel Renderer invariata: `normal_map → bump_map → autobump`. **Bound auto-derivation per Mix**: `MixDisplacement.Bound = max(A.Bound, B.Bound)`; padding AABB foglia BVH legge il bound effettivo del material risolto. **Nuovi unit test** in `MaterialDisplacementTests.cs` (9 test): same material on two entities → geometria identica; MixDisplacement at blend=0 → matches child A; at blend=1 → matches child B; at blend=0.5 → average; MixBumpMapTexture at 0/1/0.5 con output normalizzato; `displacement_enabled: false` sopprime l'altrimenti-displaced material; legacy entity-level YAML produce errore con messaggio "material-level". I 384 test pre-esistenti + 9 nuovi passano (393/393). **Nuovo showcase** `scenes/showcases/material-displacement-mix-showcase.yaml`: quattro lastre con due che condividono lo stesso material displaced (riuso), una con `displacement_enabled: false` (bypass per-istanza), e una con `porcellana_mix` Mix-displacement (vector-blend fbm/voronoi). **File toccati**: `src/RayTracer/Materials/MaterialDisplacement.cs` (new), `src/RayTracer/Textures/{IBumpMap, MixBumpMapTexture}.cs` (new), `src/RayTracer/Materials/{IMaterial, Lambertian, Metal, Dielectric, DisneyBsdf, Emissive, MixMaterial}.cs` (slot Displacement), `src/RayTracer/Scene/{SceneData, SceneLoader, ObjLoader}.cs` (schema + two-pass build + entity dispatch), `src/RayTracer/Geometry/{Mesh, Subdivision/DisplacementEngine}.cs` (IBumpMap, ApplyMix), `src/RayTracer/Core/HitRecord.cs` (IBumpMap), `src/RayTracer/Textures/BumpMapTexture.cs` (implements IBumpMap), `src/RayTracer.Tests/{BumpDisplacementCombinationTests, MaterialDisplacementTests}.cs`, 4 showcase YAML migrate + 1 nuovo, `docs/reference/{scene-reference, riferimento-scene}.md`, `docs/tutorial/{en,it}/{03-materials, 04-geometric-primitives}.md`, `README.md`, `DEVLOG.md`.

- **2026-05 — TextureTransform split: geometry vs noise.** Branch `claude/fix-marble-textures-R5Jql`. Refactor architetturale del modulo `Textures/TextureTransform.cs` per risolvere alla radice un trade-off introdotto dal commit `1475954` "LocalPoint object-locale": il singolo metodo `Apply(p, offset, rotation, seed, randomizeOffset, randomizeRotation)` accoppiava in un'unica somma `p + manualOffset + seedOffset` due concetti distinti — la trasformazione manuale dell'utente (geometricamente meaningful, parte del layout del pattern) e la decorrelazione random per-istanza (utile solo come shift sul punto di campionamento del Perlin/Voronoi). Il punto risultante veniva poi usato indistintamente sia per i termini geometrici (`dist = ||q − (q·axis)·axis||` di wood, `dot(p, axis)` di gradient, `floor(p)` di brick, `sin(scale · dot(p, axis))` di marble) sia come input per il sampling `Fbm/Turbulence/Worley`. Conseguenza: un offset random grande utile per la decorrelazione (l'originale 1000 wu = ~200 cicli decorrelati) distruggeva la geometria radiale (wood: `dist` partiva da ~1000, curvatura `1/1000 ≈ 0`, anelli paralleli su sfere piccole); la patch precedente che lo ridusse a 10 wu era un compromesso che limitava sia la decorrelazione (sfere adiacenti nello sphere-showcase vedevano regioni Perlin ravvicinate — Carrara e Calacatta apparivano come cloni dello stesso pattern noise) sia rimediava solo parzialmente al wood (curvatura `1/10`, ancora visibilmente schiacciata). **Fix architetturale**: il modulo è stato spezzato in tre helper espliciti — `ApplyManual(p, offset, rotation)` (solo trasformazioni utente, geometry-safe), `ApplyRandomRotation(p, seed, enabled)` (rotazione random per-istanza attorno all'origine, preserva `‖p‖` perché rigida → wood rings restano concentrici), e `SeedOffset(seed, enabled, magnitude=1000)` (offset random per-istanza restituito come `Vector3` separato, mai sommato al punto geometrico). La convenzione d'uso in ogni texture è ora `qGeom = ApplyRandomRotation(ApplyManual(p, Offset, Rotation), seed, RandomizeRotation)` per i termini geometrici e `qNoise = qGeom + SeedOffset(seed, RandomizeOffset)` solo per l'input del sampling. Magnitudine restaurata a 1000 wu (sicuro perché applicato solo all'input di noise/voronoi, dove non c'è geometria da deformare). **Validazione industriale**: il pattern coincide puntualmente con la convenzione adottata da Arnold (Pref/`space: object` per Pref, `user_data_*` cablato solo nell'offset interno del noise), Cycles ("Texture Coordinate → Object/Generated" per la geometria; "Object Info → Random" cablato esclusivamente nel `Vector` input del Noise node, mai sostituisce il Position globale) e RenderMan (`Pref` + `PxrManifold3D` per la geometria; `PxrAttribute → PxrVary` per la randomizzazione per-istanza — `PxrRandomTextureManifold` documenta esplicitamente che la randomizzazione "must stay confined to the sampling input" perché altrimenti rompe la continuità geometrica). **Migrazione**: tutti e 6 i texture procedurali che usavano `Apply` sono stati cablati al nuovo pattern: (a) `MarbleTexture` (sin direzionale e domain warp su `qGeom`, `Fbm/Turbulence/Ridged` su `qNoise`), (b) `WoodTexture` (`dist` radiale, `along`, knot perpPlane su `qGeom`; grain/figure noise + anisotropy compression su `qNoise`), (c) `GradientTexture` (solo `ApplyManual + ApplyRandomRotation` — nessun `SeedOffset` perché il gradient è puramente geometrico e una randomizzazione di fase clamperebbe ogni sample agli endpoint, documentato in XML doc), (d) `NoiseTexture` (pure-noise — applica tutto a `transformedP`), (e) `VoronoiTexture` (pure-noise — applica tutto a `transformedP`), (f) `BrickTexture` (grid `floor(p/bw)` su `qGeom`, weathering noise su `qNoise` — due brick adiacenti condividono la griglia ma hanno usura decorrelata), (g) `CoordinateTexture` (debug-display: switch esplicito a `ApplyManual` per rendere il contratto "mai seedato" leale). Il vecchio overload `Apply` è stato marcato `[Obsolete]`, migrato tutto, poi rimosso nello stesso PR (zero callers residui verificati con grep). **YAML pubblico invariato**: `offset:`, `rotation:`, `randomize_offset:`, `randomize_rotation:` mantengono la stessa semantica utente. Le scene legacy con `randomize_offset: true` rendono diversamente solo perché ora la decorrelazione è effettivamente forte (1000 wu invece di 10 wu, due ordini di grandezza in più di separazione tra seed adiacenti) — strettamente miglior visivo: wood rings concentrici, gradient phase stabile, marble veining direzionale invariante, noise/voronoi davvero indipendenti tra istanze. **Showcase**: il problema visivo che ha originato il refactor (sphere-showcase, fila marmi: Carrara e Calacatta che apparivano come cloni dello stesso pattern noise) si risolve automaticamente senza alcuna modifica YAML — re-render della camera `frontale` mostra adesso 6 pattern marmi chiaramente decorrelati. Le modifiche già committate al `sphere-showcase.yaml` durante il debugging (ramp 4-stop su Carrara per aggiungere grigi intermedi, `randomize_rotation: true` su tutti e 6 i marmi per garantire orientamenti vena distinti) sono ortogonali al refactor e restano valide come authoring choices. **Nuovi unit test** in `TextureTransformTests.cs` (31 test): unit-level su `ApplyManual` (identità, composizione offset+rotation), `ApplyRandomRotation` (identità per `seed = 0` o `enabled = false`, determinismo per seed, preservazione `‖p‖` su 5 seed inline-theory — la proprietà rigida che mantiene wood rings concentrici), `SeedOffset` (identità per seed/enabled = 0/false, determinismo per seed, magnitude bounded a `[0, defaultMagnitude]` componente-wise su 64 seed, separazione adiacenti `>= 50 wu` su 5 coppie consecutive di seed sphere-showcase 3001..3006 — la regression guard sul "Carrara/Calacatta uguali" bug); downstream invariants su tutte le texture: `Wood_RingsRemainConcentric_WhenRandomizeOffset` (5 seed, 4 punti su circonferenza r=0.3 nel piano XZ, output identico entro 1e-3 anche con `randomize_offset: true`); `Gradient_PhaseIsIndependentOfSeed` (4 seed, output uguale a `seed = 0` entro 1e-5); `Noise_TwoSeedsAreDecorrelated` (64 probe × 10 seed = 4 480 sample, mean absolute difference massima tra coppie `> 0.05` — empiricamente `> 0.1` a 1000 wu); `Brick_GridMembershipIsSeedInvariant` (4 seed × 10 punti, brick output identico a `seed = 0` con weathering disabilitato); `Marble_VeinAxisProducesDirectionalPattern_AcrossSeeds` (4 seed, peak/trough sample a `z = ±π/12` con `noise_strength = 0` per isolare il termine direzionale → `vp.X - vt.X > 0.5` cross-seed). Tutti i 384 test passano (353 pre-esistenti + 31 nuovi). **File toccati**: `Textures/TextureTransform.cs` (split API, `[Obsolete]` rimosso), `Textures/{MarbleTexture, WoodTexture, GradientTexture, NoiseTexture, VoronoiTexture, BrickTexture, CoordinateTexture}.cs` (7 migrazioni call-site), `RayTracer.Tests/TextureTransformTests.cs` (new), `DEVLOG.md`.

- **2026-05 — CoordinateTexture node (step 7/7 "VFX production-grade" textures) — 🎉 ROADMAP CHIUSA.** Stesso branch `claude/voronoi-texturing-vfx-gSqWR`. Settimo e ultimo step della roadmap "Texturing VFX production-grade: parità completa con Arnold/RenderMan/Cycles oltre la matematica core". Aggiunto il nodo procedurale `coordinate` che ritorna come RGB le coordinate del shading point in uno dei quattro spazi canonici dei renderer pro (Object / UV / Generated / World) — equivalente puntuale al "Texture Coordinate" node di Cycles, ai nodi `Pref`/`Pworld`/`uvCoord` di RenderMan e al node `utility` di Arnold. Due usi: (1) overlay di debug per verificare a colpo d'occhio gli unwrap UV e l'allineamento object/world space, (2) driver XYZ deterministico per pilotare un'altra texture via mix material con un sistema di coordinate esplicito al posto del sample-point object-local implicito che ogni procedurale usa di default. **Refactor architetturale: `ITexture.Value(in HitRecord rec)`**. Il nodo Coordinate ha bisogno di accedere sia a `rec.LocalPoint` (object-space, già esposto via il `p` del parametro 5-arg) sia a `rec.Point` (world-space, NON esposto dalle signature esistenti). Risolto aggiungendo un terzo overload `ITexture.Value(in HitRecord rec)` con default che inoltra esattamente `(rec.U, rec.V, rec.LocalPoint, rec.ObjectSeed, rec.Footprint)`: ogni texture pre-esistente eredita il default e si comporta bit-identicamente al pre-cycle (validato dal test `DefaultValueInRec_IsByteIdentical_To5ArgPath` su 8 tipi × 5 probe = 40 assert). I 19 callsite nei materiali (`Lambertian` ×4, `Dielectric` ×2, `Metal` ×4, `DisneyBsdf` ×9) sono stati switchati da `Albedo.Value(rec.U, rec.V, rec.LocalPoint, rec.ObjectSeed, rec.Footprint)` a `Albedo.Value(in rec)` — più conciso e abilita gli usi futuri della HitRecord ricca (es. accesso a `rec.Tangent`/`rec.Bitangent` per nodi tangent-space). `Emissive.Emit`/`EmissionAt` restano sul 4-arg legacy (non hanno HitRecord disponibile — graceful degradation: Coordinate via emission usa il path 4-arg che treat `p` come sia LocalPoint sia Point, perfettamente OK per Object/UV/Generated, World "best-effort"). **Coordinate semantica per mode**: (a) **Object** `fract(rec.LocalPoint·scale)` — stesso spazio in cui campiona ogni altra procedurale, tilable per design come fanno Noise/Marble/Wood/Voronoi (cube color-debug standard); (b) **UV** `(rec.U·scale + offset.X, rec.V·scale + offset.Y, 0)` raw senza fract — kept linear perché i primitivi consegnano già U,V ∈ [0,1] e il debug-view vuole un gradiente liscio senza tiling sulla seam line; (c) **Generated** `clamp((LocalPoint - BoundsMin)/(BoundsMax - BoundsMin), 0, 1)` — workflow "reference-space" stile RenderMan `Pref`: l'artista dichiara esplicitamente i bounds (default `[-1,1]³` = unit sphere/cube/cylinder AABB) e ogni nodo downstream vede un `[0,1]³` pulito indipendentemente da come la superficie è trasformata o displaced al render time. Guard di division-by-zero su axis degenere (`|max - min| < 1e-12` ⇒ output 0.5 invece di NaN); (d) **World** `fract(rec.Point·scale + offset)` — grid world-locked che NON segue l'oggetto (laser-grid, polvere world-aligned, "you-are-here" debug spheres). Stesso `TextureTransform.Apply` di ogni altra procedurale per `offset`/`rotation`, applicato PRIMA del fract / della normalizzazione bounds. `color_ramp:` è volutamente NON supportato (Coordinate è output identity vettoriale, non scalare mappabile su ramp 1-D — stessa convenzione delle modalità Cell/Position di Voronoi). **YAML schema**: nuovo `type: "coordinate"` con alias `coord`/`coords`/`texture_coord`/`tex_coord`/`st`; nuove chiavi `bounds_min`/`bounds_max` (List<float>?, default `[-1,-1,-1]`/`[1,1,1]`); `mode:` accetta `object`/`uv` (`st`/`parametric`)/`generated` (`gen`/`ref`/`pref`)/`world` (`pworld`/`world_space`); `scale`/`offset`/`rotation` standard. **Showcase nuovo** `scenes/showcases/coordinate-texture-showcase.yaml`: 4 sfere unit-radius identiche su pavimento neutro, una per mode (Object/UV/Generated/World), Lambertian per non lavare via la trama coordinata dietro speculare/dielectric — debug puro. Una sfera "world" a x=3.6 mostra chiaramente come la grid world-space tagli la sfera dove cadono gli interi dello spazio mondo. **Nuovi unit test** in `CoordinateTextureTests` (20 test, 12 fact + 8 theory rows): Object `fract` del LocalPoint con assert numerico esatto su `(2.25, -0.75, 0.5) → (0.25, 0.25, 0.5)`; range `[0,1]³` su 4 probe arbitrari (negative, integer-boundary, large); `scale=4` packs more bands (sample 0.125 → 0.5 di output); UV linear senza fract su `(0.3, 0.7) → (0.3, 0.7, 0)`; UV `offset` shifts both channels; Generated maps corners `±1 → 0, 1` con midpoint `0 → 0.5` su default bounds; clamp out-of-bounds; custom bounds `[0,10]³` rescale; degenerate bounds `min==max` ⇒ fallback `0.5` invece di NaN; World legge `rec.Point` ignorando `LocalPoint`; World ≠ Object on transformed point; **back-compat byte-identica `Value(in rec)` ≡ `Value(u,v,p,seed,fp)` su 8 tipi di texture × 5 probe** (SolidColor, Checker, Noise, Marble, Wood, Voronoi, Brick, Gradient); legacy 4-arg path produce risultato finito + range valido per Coordinate (no crash quando chiamata fuori contesto rec, es. da Emissive). Tutti i 353 test passano (333 pre-esistenti + 20 nuovi). **File toccati**: `Textures/CoordinateTexture.cs` (new), `Textures/ITexture.cs` (overload `Value(in HitRecord rec)` + default forwarding), `Scene/SceneData.cs` (`bounds_min`/`bounds_max`), `Scene/SceneLoader.cs` (factory entry + `ApplyTextureParams` + `ParseCoordinateMode`), `Materials/{Lambertian,Dielectric,Metal,DisneyBsdf}.cs` (19 callsite refactor `Value(rec.U,...) → Value(in rec)`), `RayTracer.Tests/CoordinateTextureTests.cs` (new), `scenes/showcases/coordinate-texture-showcase.yaml` (new), `docs/reference/{scene-reference,riferimento-scene}.md`, `docs/tutorial/{en,it}/03-materials.md`, `DEVLOG.md` (TODO genitore chiuso, voce storica). **🎉 Roadmap "Texturing VFX production-grade" CHIUSA dopo 7 step**: (1) anti-aliasing analitico via ray differentials (4 fonti: PBRT §10.1, Heidrich-Slusallek 1998, EWA Heckbert 1989, PxrVoronoise), (2) color ramp multi-stop (4 interp: Linear/Smoothstep/Ease/Constant), (3) Smooth Voronoi (IQ log-sum-exp soft-min in doppia precisione), (4) Musgrave HeteroTerrain + HybridMultifractal (Ebert/Musgrave/Peachey/Perlin §16.3), (5) Marble/Wood studio-quality (secondary wave + auto-orthogonalizzazione, radial anisotropy, knot density con anelli concentrici), (6) F3/F4 + F3−F1 + Position output Voronoi (Cycles/Houdini parity), (7) CoordinateTexture node (Cycles/RenderMan Pref workflow). Il renderer 3D-Ray ora ha **parità completa di feature texturing** con i tre principali renderer di production VFX.

- **2026-05 — F3/F4 e output Voronoi estesi (step 6/7 "VFX production-grade" textures).** Branch `claude/voronoi-texturing-vfx-gSqWR`. Sesto step della roadmap "Texturing VFX production-grade: parità completa con Arnold/RenderMan/Cycles oltre la matematica core": il nodo `voronoi` ora espone l'intero set di canali "production" che Cycles, RenderMan PxrVoronoise e Houdini Voronoi offrono di default — F3, F4, F3−F1 e Position — sopra agli F1/F2/F2−F1/F1+F2/Cell pre-esistenti. Costo zero sulle 27 celle: la finestra 3×3×3 viene già scansionata da `WorleyNoise.Evaluate`, l'unico extra è una insertion-sort a 4 slot (3 comparazioni extra/cella) e il carry della feature-position del cell F1. **Nuovo metodo** `WorleyNoise.EvaluateExtended(p, metric, randomness, out f1, out f2, out f3, out f4, out cellId, out featurePosition)`: scaffolding identico al loop O(27) di `Evaluate` ma con un insertion-ladder a 4 livelli; il `cellId` e la `feature` del cell F1 vengono aggiornati solo nel branch `d < b1`, garantendo coerenza topologica fra `Cell` e `Position` su ogni input. Il ladder è progettato per riprodurre **bit-identicamente** F1, F2 e cellId della legacy `Evaluate` su ogni input (stesso ordine di compare, stessa semantica di swap sui primi due slot — verificato dal test `Extended_F1F2CellId_BitIdentical_ToLegacyEvaluate` su tutte e 4 le metriche). **Nuovi `OutputMode`** `F3`, `F4`, `F3MinusF1`, `Position` (la enum legacy `F1/F2/F2MinusF1/F1PlusF2/Cell` resta invariata). **Dispatch** in `VoronoiTexture.SampleAtP`: i nuovi 4 output entrano in un percorso dedicato che chiama `EvaluateExtended` e **ignora `Smoothness`** — convenzione di Cycles per i canali di topologia discreta: il soft-min log-sum-exp è semanticamente applicabile solo agli scalar field continui (F1/F2 e derivati), mentre F3/F4 sono indexing della closest-N e Position è un'identità vettoriale. Il test `VoronoiTexture_Smoothness_DoesNotAffectExtendedModes` blocca regressioni su questo contratto. **Normalizzazione** estesa: F3 e F4 dividono per lo stesso `maxD` metric-specific di F1/F2 (clamp a `[0,1]` cattura gli overflow occasionali da feature sul corner del 27-cell window); `F3MinusF1` usa la stessa compressione `sqrt(Δ/(0.5·maxD))` di `F2MinusF1` (la risposta "Distance to Edge" di Cycles), producendo una banda border più larga e a frequenza più bassa di F2−F1 — la disuguaglianza spettrale è validata dal test `F3MinusF1_WiderBandThan_F2MinusF1` su griglia 8³ (mean(F3−F1) > mean(F2−F1)). **Position** ritorna l'XYZ del feature point F1 cellettato in `[0,1]³` via `feature - floor(feature)` — coordinata cell-locale che con randomness=1 coincide direttamente col jitter table del cell-id e che con randomness=0 collassa a (0.5,0.5,0.5) per definizione; bypassa il `color_ramp` (vector identity output, non scalare — stessa convenzione di Cycles e RenderMan, e protetto dal test `Position_BypassesColorRamp`). **YAML**: nessuna nuova chiave — sono nuovi valori dell'enum `output:` già esistente. `SceneLoader.ParseVoronoiOutput` accetta gli alias `f3`, `f4`, `f3_minus_f1`/`f3-f1`/`wide_crackle`/`border`, `position`/`feature_position`/`feature`/`p`. **Back-compat byte-identica**: ogni scena legacy che non usa i nuovi alias di `output:` produce output identico al pre-cambio — il dispatch su `needsExtended` skippa l'evaluator esteso per `F1/F2/F2MinusF1/F1PlusF2/Cell` e quei branch entrano nel path identico al pre-cycle (`EvaluateSmooth` se smoothness > 0, altrimenti `Evaluate`). **Showcase nuovo** `scenes/showcases/voronoi-extended-outputs-showcase.yaml`: 6 sfere identiche su pavimento neutro (stesso seed, stesso scale 5.5, stessa metric Euclidean — solo `output` varia). Riga A: `f1` reference / `f2_minus_f1` crackle classico / `f3_minus_f1` crackle largo. Riga B: `f3` / `f4` / `position`. Color ramp 3-stop dove applicabile (warm-tone per le distanze, cool-tone per F3/F4); `position` senza ramp per esibire il bypass. **Nuovi unit test** in `VoronoiExtendedOutputsTests` (20 test eseguiti, 4 theory rows + 16 fact): ordinamento `F1≤F2≤F3≤F4` su ogni input; back-compat byte-identica di F1/F2/cellId fra `Evaluate` e `EvaluateExtended` su tutte e 4 le metriche; `featurePosition` letterale (la distanza `|p − feature|` Euclidea coincide con F1 entro 1e-5); stabilità della `featurePosition` su un piccolo offset dentro la stessa cella (no flickering quando `randomness=0`); back-compat byte-identica delle 5 modalità legacy su 10 probe per modalità; range RGB `[0,1]` + assenza di NaN/Inf su griglia 7×7×7 per ogni nuovo `OutputMode`; distinctness `F3 ≠ F2` e `F4 ≠ F3` su almeno un probe (regression guard contro alias accidentali); banda media `F3−F1 > F2−F1` su griglia 8³; `Position` deterministica per cella (stesso valore su due probe interni allo stesso cell), variazione fra celle adiacenti, bypass `ColorRamp`, coerenza con `Cell` (due probe con cell diversi ⇒ Position diversa); `Smoothness > 0` non perturba nessuno dei 4 nuovi canali. Tutti i 333 test passano (313 pre-esistenti + 20 nuovi). **File toccati**: `Core/WorleyNoise.cs` (nuovo metodo `EvaluateExtended` + doc), `Textures/VoronoiTexture.cs` (4 nuovi OutputMode + dispatch needsExtended + normalizzazione F3/F4 + Position fract-cell + bypass ramp), `Scene/SceneLoader.cs` (ParseVoronoiOutput esteso), `RayTracer.Tests/VoronoiExtendedOutputsTests.cs` (new), `scenes/showcases/voronoi-extended-outputs-showcase.yaml` (new), `docs/reference/{scene-reference, riferimento-scene}.md`, `docs/tutorial/{en,it}/03-materials.md`. **Step rimanente del TODO genitore** (7: CoordinateTexture node): ciclo successivo.

- **2026-05 — Libreria materiali "studio-quality" + documentazione approfondita (step 5/7 follow-up #2).** Stesso branch. Pubblicato il catalogo di marmi e legni pre-cotti con i nuovi parametri di step 5 nelle librerie `scenes/libraries/materials/{stones, woods}.yaml`, e steso un tutorial walkthrough esteso nei doc EN/IT. **Libreria pietre** — 10 nuovi marmi con suffisso `_studio` (Disney + Lambertian per pavimenti/sfondo, +variants `_lucido` con clearcoat 0.85+ per top cucina): `dis_carrara_studio`, `dis_calacatta_studio`, `dis_statuario_studio`, `dis_arabescato_studio` + corrispettivi lucidi, `dis_port_laurent_studio_lucido`, `dis_rosso_levanto_studio_lucido`. Ogni materiale usa `vein_sharpness` (3-5), `secondary_wave` per cross-veining su Calacatta/Statuario/Arabescato/Port Laurent/Rosso Levanto, e `color_ramp` 3-4 stop con la convenzione corretta post-fix (position 0 = vena rara, position 1 = base dominante). **Libreria legni** — 11 nuovi legni con suffisso `_studio`: `dis_acero_curly_studio` (figure band 1.8), `dis_acero_birdseye_studio` (figure scale alta), `dis_acero_sapwood_studio` (ramp 4-stop sapwood/heartwood), `dis_mogano_flame_studio` (figure intensa + clearcoat 0.75), `dis_quercia_quartato_studio` (radial_anisotropy 3), `dis_frassino_quartato_studio` (radial_anisotropy 2.5), `dis_pino_nodoso_studio` (knot_density 1.0 + scale 6 per anelli interni visibili), `dis_abete_nodoso_studio`, `dis_noce_burl_studio` (figure + knots + clearcoat). **Documentazione tutorial** — nuovo sotto-capitolo `3.8.1` "Marble & Wood Studio Lookdev — A Practical Walkthrough" nei file `tutorial/{en,it}/03-materials.md`: 10-step guide che copre setup illuminazione (cielo dark + key direzionale forte per dominare il riflesso speculare dei marmi lucidi), tabella personalità marmi (Carrara/Calacatta/Statuario/Arabescato/Port Laurent/Rosso Levanto con parametri tipici di sharpness + secondary_wave + distortion + ramp), convenzione di sharpness post-fix (`t = 1 − (1−t)^k`, area dominante = base, ramp position 0 = vena rara), secondary wave + auto-orthogonalizzazione, roughness/clearcoat layering, tabella cut di legno (plain-sawn / quartersawn / curly / bird's-eye / flame / knotty / burl con parametri figure_strength + radial_anisotropy + knot_density), ring_sharpness vs scale, autorialità anelli dei nodi con ramp 4-stop, randomizzazione per instancing (`randomize_offset`/`randomize_rotation`), e listato finale del catalogo `_studio`. **Documentazione reference** — sezione "Production-quality marble & wood — recipe book" (EN) / "Marmi e legni production-quality — ricettario" (IT) con snippet copia-incolla minimali per ciascun marmo/legno + checklist illuminazione (sky flat near-black, roughness 0.30-0.34 per lucido, direzionale + fill + rim). **File toccati**: `scenes/libraries/materials/{stones, woods}.yaml` (header doc esteso + nuove sezioni STUDIO-QUALITY), `docs/reference/{scene-reference, riferimento-scene}.md`, `docs/tutorial/{en,it}/03-materials.md` (nuovo §3.8.1).

- **2026-05 — Marble + Wood studio-quality, lookdev review (step 5/7 follow-up).** Stesso branch. Review post-implementazione del showcase: la prima versione del file aveva due problemi visivi che impedivano di leggere correttamente le texture: (a) le sfere di legno della riga posteriore erano coperte da quelle di marmo davanti (camera a `z=-8`, riga marmi `z=-1` ⇒ occlusione totale di `z=+1`), e (b) i marmi a `roughness 0.18-0.22` riflettevano talmente tanto del cielo gradient che la trama del marble veniva lavata via in un beige-blu uniforme. **Fix layout**: fila unica di 6 sfere alternate marmo/legno con seed unici per entità, camera arretrata a `z=-10.5` con FOV 56°. **Fix illuminazione/sfondo**: cielo flat near-black `[0.001, 0.001, 0.0012]` (niente riflessione ambientale a lavare i marmi); key direzionale a 6.5 di intensità (forza dominante sulla riflessione speculare); fill freddo +0.95-tinto e rim caldo dal retro per stacco silhouette. Marble roughness alzato a 0.30-0.34 (lucido ma non a specchio). **Bug fix sostanziale: sharpening invertito di `MarbleTexture`.** `vein_sharpness > 1` produceva il pattern OPPOSTO a quello dei renderer pro: mathematica `t = t^k` spinge la media campionaria a `1/(k+1)` ⇒ verso lo 0 (vein), così MAGGIOR PARTE della superficie veniva colorata col colore di vena (scuro) e solo sottili bande mostravano la base (chiara). Real Carrara/Calacatta/Statuario sono il contrario: base bianca DOMINANTE con venature scure SOTTILI, esattamente come Cycles `Width`, Arnold marble2 `vein_width`, RenderMan PxrMarble `veinFalloff`. Cambio: `t = 1 - (1-t)^k` — ora sharpness alta restringe la regione di vena e allarga la base (media a `k/(k+1)` ⇒ verso 1). `vein_sharpness = 1` resta bit-identica al legacy. Cambio confermato dal nuovo test `Marble_HighSharpness_BaseColorDominates_VeinIsThin` e dal test di monotonicità `Marble_HigherSharpness_NarrowsTheVein` (avg luminance a sharpness 1 < 4 < 8). Ho anche aumentato la soglia knot da `0.18 · density` a `0.45 · density` perché con la prima i nodi erano troppo piccoli per mostrare gli anelli concentrici al loro interno (Arnold `knots`/RM PxrWoodKnot fanno nodi grandi per default). I test esistenti continuano a passare (309/309: 294 pre-step-5 + 13 step-5 + 2 nuovi sharpening). **Beneficio collaterale**: `textures-pro-showcase.yaml` aveva una ramp 4-stop con commento "vein dark / gold transition / base warm white" ma con la sharpening invertita renderizzava marble per lo più scuro/oro con sottili venature bianche; ora rende come il commento indica, marmo Statuario chiaro con vene scure e lampi dorati. **File toccati nel follow-up**: `Textures/MarbleTexture.cs` (sharpening fix), `Textures/WoodTexture.cs` (knot threshold 0.18→0.45), `scenes/showcases/marble-wood-studio-showcase.yaml` (layout + lighting + materials rework), `RayTracer.Tests/MarbleWoodStudioTests.cs` (+2 sharpening tests).

- **2026-05 — Marble + Wood "studio quality" (step 5/7 "VFX production-grade" textures).** Branch `claude/smooth-voronoi-texturing-nqCt1`. Quinto step della roadmap "Texturing VFX production-grade: parità completa con Arnold/RenderMan/Cycles oltre la matematica core": MarbleTexture e WoodTexture salgono al livello "lookdev VFX" con controlli opt-in che li portano a parità feature con Arnold `marble2` + `knots`, RenderMan PxrMarble + PxrWoodKnot, e i nodi Wood/Marble di Cycles. Tutti i nuovi knob hanno default no-op verificati da test ⇒ ogni scena legacy renderizza byte-identica. **Marble — secondary wave**: blocco YAML opzionale `secondary_wave: { axis, frequency, strength }`. Quando `strength > 0` viene aggiunta una seconda sinusoide al termine di vena: `sin(w1) + strength · sin(w2)`, rinormalizzato per `(1 + strength)` in modo che l'argomento del color lerp/ramp resti in [0, 1]. L'asse secondario è auto-ortogonalizzato contro il primario al sample-time (proiezione + Gram-Schmidt con fallback su axis ⊥ help-vector quando l'utente passa accidentalmente un axis perfettamente collineare al primario) — questa scelta significa che anche `secondary_wave.axis: [0, 0, 1]` con `vein_axis: [0, 0, 1]` produce comunque cross-veining visibile, mai un silent no-op. Sblocca marmi Statuario/Calacatta/Arabescato (vena dorata/grigia/nera lungo due direzioni) e qualunque cross-pattern artistico. **Wood — quattro nuovi knob studio-quality**: (1) **bande grain + figure** indipendenti — `GrainScale` (default 1.0, no-op back-compat) e `FigureScale` (default 0.25) moltiplicano il sample point del noise rispettivamente per la banda alta-freq (fibra dentro l'anello) e bassa-freq (ondulazione plank-wide). `FigureStrength` (default 0.0 → banda figure disattivata) pesa la banda figure additivamente a `NoiseStrength·grain`. La banda figure viene campionata con offset Perlin decorrelato (`+ (127.13, 89.41, 53.27)`) così grain e figure non lockstep — sblocca curly maple, flame mahogany, bird's-eye. (2) **`RadialAnisotropy`** (default 0.0): proietta `q` sulla direzione radiale locale `r_hat = radial / |radial|`, comprime la componente radiale del sample-point per il noise di un fattore `1/(1 + RadialAnisotropy)`, lascia invariate axial e tangenziale. Implementazione safe sul trunk axis (`|radial| < 1e-6f` ⇒ skip path, no division by zero — coperto da `Wood_AnisotropyAtTrunkAxis_NoNaN` test). 0 è bit-identica al legacy. Sblocca rovere quartato (anisotropia alta, fibre stirate radialmente) vs piano-sawn (anisotropia 0). (3) **`KnotDensity`** (default 0.0): quando > 0, una sample point del WorleyNoise in piano-⊥-axis space (knot extend lungo l'asse del tronco, come le branche reali) determina la vicinanza al feature point più vicino di un nodo; con `kF1 < 0.18 · KnotDensity` siamo dentro il nodo, il ring center locale viene blendato con il knot center (concentric rings around the knot — esatto modello Arnold/RenderMan) e si aggiunge un dark heart `t *= (1 - mask²)` applicato POST ring-sharpening, così il nodo resta nero indipendentemente dalla banda di latewood/earlywood in cui cade. Frequency del Voronoi modulata da density (`0.35 + 0.65·density`) così density=0.1 dà nodi grossi e radi, density=1 dà nodi piccoli e numerosi. **YAML schema** esteso con `TextureData.SecondaryWave` (POCO `SecondaryWaveData { axis, frequency, strength }`), `TextureData.GrainStrength` (alias forward-compat per `noise_strength`, last-write-wins se entrambi presenti), `TextureData.GrainScale`, `TextureData.FigureScale`, `TextureData.FigureStrength`, `TextureData.RadialAnisotropy`, `TextureData.KnotDensity`. `SceneLoader.ApplyTextureParams` parsa tutti i nuovi campi con clamping appropriato (radial_anisotropy/figure_strength ≥ 0; knot_density ∈ [0, 1]). **Showcase nuovo** `scenes/showcases/marble-wood-studio-showcase.yaml`: 6 sfere su pavimento neutro, due file. Riga A: Carrara legacy (single-axis) / Calacatta (secondary 0.45 + ramp 4-stop oro/avorio/cool) / Arabescato (secondary 0.7 + distortion). Riga B: rovere quartato (anisotropy 2.5 + ramp 3-stop) / curly maple (figure 1.6 a scale 0.18) / knotty pine (figure 0.6 + knot_density 0.7 + ramp 4-stop con cuore-nodo). **Nuovi unit test** in `MarbleWoodStudioTests` (13): Marble `secondary_strength = 0` bit-identica al legacy (varying axis/frequency); secondary > 0 cambia output; secondary axis collineare ⇒ auto-orthogonalised (verifica con `VeinAxis: Z` e `SecondaryAxis: Z` che produce comunque differenza); range RGB [0, 1] preservato con secondary attivo + sharpness 4. Wood: tutti i 5 nuovi knob ai default ⇒ bit-identica al legacy; `figure_strength > 0` cambia output; `grain_scale = 1` ≡ legacy ∧ `grain_scale ≠ 1` cambia; `radial_anisotropy = 0` bit-identica; `radial_anisotropy > 0` cambia; **`radial_anisotropy = 5` su strip sul trunk axis ⇒ no NaN/Inf** (regression guard sulla divisione per `dist`); `knot_density = 0` bit-identica; `knot_density = 1` ⇒ brightness media più bassa (i nodi scuriscono); worst-case stress (tutti i knob al massimo) ⇒ no NaN/Inf ∧ RGB ∈ [0, 1]. Tutti i 307 test passano (294 pre-esistenti + 13 nuovi). **File toccati**: `Textures/{MarbleTexture, WoodTexture}.cs`, `Scene/{SceneData, SceneLoader}.cs`, `RayTracer.Tests/MarbleWoodStudioTests.cs` (new), `scenes/showcases/marble-wood-studio-showcase.yaml` (new), `docs/reference/{scene-reference, riferimento-scene}.md`, `docs/tutorial/{en,it}/03-materials.md`. **Step rimanenti del TODO genitore** (6-7: F3/F4 Voronoi outputs + Position output, CoordinateTexture node): cicli successivi.

- **2026-05 — Musgrave multifractal completo (step 4/7 "VFX production-grade" textures).** Branch `claude/smooth-voronoi-texturing-nqCt1`. Quarto step della roadmap "Texturing VFX production-grade: parità completa con Arnold/RenderMan/Cycles oltre la matematica core": `NoiseTexture` ora espone le due varianti multifrattali canoniche dal capitolo "Procedural Fractal Terrains" di Musgrave (Ebert/Musgrave/Peachey/Perlin, *Texturing &amp; Modeling: A Procedural Approach*, 3rd ed. §16.3) — i due frattali che producono terreno eroso "vero" e roccia stratificata, irraggiungibili con fBm puro perché fBm ha statistica identica a ogni quota mentre i multifrattali Musgrave variano localmente in funzione del valore accumulato. Allineamento puntuale a Cycles "Musgrave Texture" (Hetero Terrain / Hybrid Multifractal), RenderMan terrain shaders, MojoWorld e Houdini turbulence in modalità heterogeneous. **Nuovi metodi** `Perlin.HeteroTerrain(p, octaves, lacunarity, H, offset)` (§16.3.3) e `Perlin.HybridMultifractal(p, octaves, lacunarity, H, offset)` (§16.3.4): peso spettrale per ottava `lacunarity^(-i·H)` calcolato inline come moltiplicatore costante (no allocazione, no `exponent_array` pre-baked — il compilatore JIT inlinea il `MathF.Pow` invariant fuori dal loop). HeteroTerrain: prima ottava `value = offset + Noise(p)`, ricorsione `value += (Noise(p_i) + offset) · spectral_weight_i · value` (l'amplificazione "altezza-correlata" che produce creste rugose dove il segnale è alto e valli lisce dove è basso). HybridMultifractal: stessa scaffolding ma con un running `weight` clampato a 1 che accumula `weight *= signal` — produce stratificazioni rocciose con picchi netti. **Nuovi `NoiseTexture.NoiseKind`**: `HeteroTerrain`, `HybridMultifractal`; dispatch nel switch di `ValueCore`. **Nuove property** `NoiseTexture.FractalIncrement` (H, default 1.0) e `NoiseTexture.FractalOffset` (offset, default 0.7) — entrambe ignorate dagli altri `NoiseKind` per design (lookup-table di solo dispatch). **YAML schema** esteso con `TextureData.FractalIncrement` (`fractal_increment`) e `TextureData.FractalOffset` (`fractal_offset`); `SceneLoader.ApplyTextureParams` assegna i due campi al `NoiseTexture`. `ParseNoiseKind` accetta gli alias `hetero_terrain | heteroterrain | hetero | heterogeneous` e `hybrid_multifractal | hybridmultifractal | hybrid | multifractal`. **Convenzione di back-compat**: omettendo `noise_type: hetero_terrain|hybrid_multifractal` (o con qualunque altro tipo), `FractalIncrement` e `FractalOffset` sono no-op — i 5 noise legacy (perlin/fbm/turbulence/ridged/billow) producono output bit-identico al pre-cambio. **Output range**: i valori grezzi Musgrave dipendono da H, offset e octaves e possono uscire da [0, 1]; il clamp `[0, 1]` esistente in `NoiseTexture.ValueCore` viene applicato come per gli altri kind. Per controllo fine sui valori alti (saturazione dei picchi) gli artisti possono attaccare un `color_ramp:` con stop personalizzati — esattamente come fa Cycles sul nodo Musgrave. **Showcase nuovo** `scenes/showcases/musgrave-multifractal-showcase.yaml`: quattro pannelli quad verticali fianco a fianco con stesso color ramp 7-stop terrain-style (acqua/spiaggia/erba/roccia/neve) — A fBm reference (statistica omogenea), B hetero_terrain (creste alte/valli lisce), C hybrid_multifractal (strati rocciosi), D hetero_terrain con `fractal_offset: 0.2` (montagne ovunque, niente sea-level). **Nuovi unit test** in `MusgraveMultifractalTests` (10 fact + 5 inline theory rows = 15 test eseguiti): HeteroTerrain a 1-octave `≡ offset + Noise(p)` (sanity baseline), HybridMultifractal a 1-octave idem, HeteroTerrain a 2-octave matcha esattamente l'unroll analitico Musgrave (spectral_weight + running-value multiplier verificati), HybridMultifractal a 2-octave matcha l'unroll con clamp `weight ≤ 1`, determinismo bit-identico su stesso seed, output finito su 5 valori di H ∈ [0, 1.5] (no NaN/Inf — copre regressioni dove `weight ≥ 1` unclamped esplode il segnale ibrido), monotonia roughness ↘ H (variance(H=0.1) ≥ 2× variance(H=1.5) su grid 64×64 — comportamento richiesto dagli artisti), offset agisce come shift additivo puro a 1-octave, `NoiseTexture` HeteroTerrain ≠ Fbm su almeno un probe (no-op check), HybridMultifractal ≠ Fbm ∧ ≠ HeteroTerrain (sono pattern distinti), back-compat byte-identica del `NoiseTexture` legacy quando i nuovi `FractalIncrement`/`FractalOffset` variano ma `NoiseType` non è uno dei due nuovi. Tutti i 294 test passano (279 pre-esistenti + 15 nuovi). **File toccati**: `Core/Perlin.cs` (HeteroTerrain + HybridMultifractal), `Textures/NoiseTexture.cs` (kind + property), `Scene/{SceneData, SceneLoader}.cs` (YAML fields + parser), `RayTracer.Tests/MusgraveMultifractalTests.cs` (new), `scenes/showcases/musgrave-multifractal-showcase.yaml` (new), `docs/reference/{scene-reference, riferimento-scene}.md`, `docs/tutorial/{en,it}/03-materials.md`. **Step rimanenti del TODO genitore** (5-7: Marble/Wood studio-quality, F3/F4 Voronoi outputs, CoordinateTexture node): cicli successivi.

- **2026-05 — Smooth Voronoi (step 3/7 "VFX production-grade" textures).** Branch `claude/smooth-voronoi-texturing-nqCt1`. Terzo step della roadmap "Texturing VFX production-grade: parità completa con Arnold/RenderMan/Cycles oltre la matematica core": il nodo `voronoi` può ora produrre celle C∞ via soft-min log-sum-exp (Inigo Quilez, "Smooth Voronoi") invece del `min()` discreto, sbloccando il look "smooth crackle" senza alias a step lungo le creste — superficie tipica di pelle di rettile levigata, ciottoli arrotondati dall'acqua, cuoio poro-chiuso, marmo Bianco P levigato. Allineamento puntuale a Cycles "Smooth F1", Houdini Voronoi Smoothness e RenderMan PxrVoronoise blend factor. **Nuovo metodo** `WorleyNoise.EvaluateSmooth(p, metric, randomness, smoothness, out f1, out f2, out cellId)`: con `smoothness == 0` ritorna esattamente i risultati di `Evaluate` (back-compat byte-identica); con `smoothness > 0` calcola `k = 20/smoothness` e rimpiazza il `min` con `-log(Σ exp(-k·(d_i − hardF1)))/k` (rebase sulla distanza hard più vicina per stabilità numerica — nessun argomento di `exp()` supera mai 0). F2 viene costruito dalla stessa accumulazione escludendo il peso dominante (tracciato per indice, non per sottrazione, evitando la cancellazione catastrofica `sumAll − 1` quando il secondo termine è sotto float32 epsilon contro 1.0). L'accumulatore lavora in **doppia precisione** così k·diff può crescere a centinaia di decadi senza perdita di significato. **Soglia di underflow**: quando `sumRest < 1e-300` (k estremamente grande, i.e. smoothness → 0) cade analiticamente sul hard F2 — il limite corretto. **Clamp di sicurezza** `softF2 ≤ hardF2`: vincolo Jensen-derivato (soft-min ≤ min), implementato come safety net contro pseudo-overshoot da arrotondamento. **Nuova property** `VoronoiTexture.Smoothness` (default `0f`, clampata a `[0, 1]` dal loader); `SampleAtP` dispatcha su `Smoothness > 0f → EvaluateSmooth`. La modalità `OutputMode.Cell` è volutamente esclusa dal soft-min (il cell-ID è discreto per natura — stessa scelta di Cycles). **YAML schema** esteso con `TextureData.Smoothness` (`float?`); `SceneLoader.ApplyTextureParams` clamp + assign quando il valore è presente. Convenzione di back-compat: omettendo `smoothness:` (o impostandolo a `0`), il sample è bit-identico al pre-cambio per ogni scena legacy. **Showcase nuovo** `scenes/showcases/smooth-voronoi-showcase.yaml`: tre sfere identiche fianco a fianco con stesso seed (struttura Voronoi coincidente, solo `smoothness` varia: 0.0 / 0.3 / 0.7), output `f2_minus_f1` con color ramp 4-stop per esaltare le creste — confronto visivo diretto dell'effetto di smussamento. **Nuovi unit test** in `SmoothVoronoiTests` (11): `Smoothness == 0` bit-identica al hard `Evaluate`, `smoothness → 0` converge a hard entro 5e-3, contratti `softF1 ≤ hardF1` e `softF2 ≥ softF1` su 4 livelli di smoothness, 1-Lipschitz analitica di softF1 su scan-line a 4 096 campioni (la metrica Euclidea su un singolo feature è 1-Lipschitz, soft-min lo eredita), 2-Lipschitz analitica di softF2 − softF1, riduzione del worst-case slope locale di F2−F1 hard→soft (smoothness fa davvero qualcosa), back-compat byte-identica del wrapper `VoronoiTexture` con `Smoothness` default, mutazione effettiva con `smoothness > 0` sulla modalità `f2_minus_f1`, immunità della modalità `cell` al parametro. Tutti i 279 test passano (268 pre-esistenti + 11 nuovi). **File toccati**: `Core/WorleyNoise.cs` (nuovo metodo `EvaluateSmooth`), `Textures/VoronoiTexture.cs` (property `Smoothness` + dispatch), `Scene/{SceneData, SceneLoader}.cs` (campo YAML `smoothness`), `RayTracer.Tests/SmoothVoronoiTests.cs` (new), `scenes/showcases/smooth-voronoi-showcase.yaml` (new), `docs/reference/{scene-reference, riferimento-scene}.md`, `docs/tutorial/{en,it}/03-materials.md`. **Step rimanenti del TODO genitore** (4-7: Musgrave HeteroTerrain/HybridMultifractal, Marble/Wood studio-quality, F3/F4 Voronoi outputs, CoordinateTexture node): cicli successivi.

- **2026-05 — Color ramp multi-stop (step 2/7 "VFX production-grade" textures).** Branch `claude/color-ramp-multi-stop-ZlQ5y`. Secondo step della roadmap "Texturing VFX production-grade: parità completa con Arnold/RenderMan/Cycles oltre la matematica core": le procedural texture possono ora pilotare il colore finale tramite un gradiente multi-stop con interpolazioni multiple per segmento, invece dell'implicito `Vector3.Lerp(colorA, colorB, t)` a due colori. Allineamento puntuale a Cycles `ColorRamp` node, Arnold `ramp_rgb` e RenderMan `PxrRamp`. **Nuova classe** `Textures/ColorRamp` (sorted-stops + binary search lookup, 4 modi di interpolazione canonici: `Linear`, `Smoothstep` Hermite cubico C¹, `Ease` Perlin smootherstep C², `Constant` step function; il modo di ogni stop descrive il segmento **in uscita** verso il prossimo stop come in Blender; posizioni clampate a `[0,1]`, sort automatico ascendente, stop coincidenti producono hard break — trucco da artista per transizioni nette). **Esposto** come property `ColorRamp? ColorRamp` opzionale su `NoiseTexture`, `MarbleTexture`, `WoodTexture`, `VoronoiTexture`, `GradientTexture`; quando set, il `Vector3.Lerp` finale viene sostituito da `ramp.Sample(t)`. `BrickTexture` lasciata indipendente per design (ha tre colori discreti — mortar / brick-A / brick-B — non un parametro scalare). **YAML schema** esteso con `TextureData.ColorRamp` (`List<ColorRampStopData>?`) + nuovo POCO `ColorRampStopData { position, color, interp }`. **`SceneLoader.BuildColorRamp`** parsa la lista, valida (skip degli stop con colore < 3 componenti + warning deferred), `ApplyTextureParams` chiama `BuildColorRamp` su tutti i 5 tipi target. Convenzione di precedenza: `color_ramp:` vince su `colors:` quando entrambi presenti; omettendo `color_ramp:` ogni scena pre-cambio rende byte-identica. **Sblocca** marmo Statuario / Calacatta (vena → mid-tone dorato → base → undertone, 4+ stop), wood sapwood/heartwood/knot a 3 colori, gradienti tramonto fotorealistici (sun core → orange → magenta → twilight blue), bande toon-shade, false-color heat-map. **Nuovi unit test** in `ColorRampTests` (16 test): two-stop linear ramp ≡ `Vector3.Lerp` bit-for-bit, clamping fuori range, single-stop costante, sort automatico, position clamp a `[0,1]`, constant interp è step function, smoothstep ≡ `3t² − 2t³` analiticamente, ease ≡ `6t⁵ − 15t⁴ + 10t³` analiticamente, coincident stops → hard break, empty stops → ArgumentException, NoiseTexture senza ramp ≡ comportamento legacy, NoiseTexture+3-stop visita il colore intermedio, Marble/Wood/Voronoi/Gradient: il ramp single-stop a colore impossibile dal lerp constructor bypassa correttamente i colori del costruttore. Tutti i 268 test passano (252 pre-esistenti + 16 nuovi). **File toccati**: `Textures/{ColorRamp.cs (new), NoiseTexture, MarbleTexture, WoodTexture, VoronoiTexture, GradientTexture}.cs`, `Scene/{SceneData, SceneLoader}.cs`, `RayTracer.Tests/ColorRampTests.cs` (new), `scenes/showcases/textures-pro-showcase.yaml` (pavimento marmo + sfera gradient ora con ramp), `docs/reference/{scene-reference, riferimento-scene}.md`, `docs/tutorial/{en,it}/03-materials.md`. **Step rimanenti del TODO genitore** (3-7: smooth Voronoi, Musgrave HeteroTerrain/HybridMultifractal, Marble/Wood studio-quality, F3/F4 Voronoi outputs, CoordinateTexture node): cicli successivi.

- **2026-05 — Texture filtering analitico via ray differentials (step 1/7 "VFX production-grade" textures).** Branch `claude/analytic-antialiasing-filter-36oaF`. Primo step della roadmap "Texturing VFX production-grade: parità completa con Arnold/RenderMan/Cycles oltre la matematica core": le procedural texture ora hanno informazione di footprint analitico al sample-time e possono pre-integrare il loro contributo invece di point-sampleare e produrre aliasing/moiré/shimmer a distanza. Allineamento puntuale ad Arnold (`Arnold ray differentials`), RenderMan (`RixShadeContext::du`/`dv`/`dPdu`) e Cycles (`Texture Coordinate` con derivate). **Nuovo modulo core** `Core/RayDifferential.cs` (auxiliary +x/+y rays sul plane di proiezione, PBRT §6.2.3), `Core/FilterFootprint.cs` (carry `(∂P/∂x, ∂P/∂y, ∂u/∂x, ∂v/∂x, ∂u/∂y, ∂v/∂y)`), `Core/FootprintMath.cs` (proiezione tangent-plane PBRT §10.1.1 + solver 3×2 della UV partials con largest-magnitude 2×2 sub-system per condizionamento ottimo). **`Ray` struct esteso** con `RayDifferential? Differentials` e flag `HasDifferentials` (default false → back-compat byte-identica per shadow / NEE / BSDF bounces che non portano differentials, l'antialiasing oltre il primo hit resta stocastico per design). **`HitRecord` esteso** con `DpDu`, `DpDv` (parametric partials proper-magnitude per primitiva — popolati su `Sphere` 2π·R·sinθ, `Quad` U,V, `Triangle` edges; fallback a unit Tangent/Bitangent per le altre, conservativo under-blur ma mai over-blur) e `Footprint` (consumato dalle texture filtered tramite il nuovo overload `ITexture.Value(u, v, p, seed, in FilterFootprint)` con default DIM forwarding al `Value` 4-arg point-sampled — pass-through totale per ogni ITexture che non override). **Camera** estesa con `GetRayWithDifferentials(s, t, dsdx, dtdy)`: aux rays attraverso pixel +x/+y (dsdx/dtdy scalano con `1/√spp` come PBRT §10.1.5, così il filtro analitico si stringe quando spp cresce). **`Transform.Hit` propaga differentials in object-space** tramite l'inverse matrix (Jacobiana = matrice stessa per affini) e applica `DpDu`/`DpDv` con la forward; il footprint resta in object space — coerente con `rec.LocalPoint` usato dalle procedurali. **`Renderer.TraceRay` calcola il footprint** una sola volta dopo Hit success quando `currentRay.HasDifferentials` (skip per ray segreti); skip-loop CameraInvisible preserva differentials. **Implementazioni filtered**: `NoiseTexture` clamp ottave a `i_max = ⌊log_{lacunarity}(1/(2·scale·footprintExtent))⌋ + 1` (Heidrich-Slusallek 1998 §4 — sopra Nyquist le ottave alte vengono droppate analiticamente, ogni `Fbm`/`Ridged`/`Billow`/`Turbulence` accetta `octaveOverride`); `VoronoiTexture` supersampling adattivo 1/4/9/16 jitter samples in footprint con hash deterministico su (objectSeed, p) — niente moiré bands su superfici uniformi (PxrVoronoise contract); `ImageTexture` mipmap pyramid (build 2×2 box filter in linear-light space at construction) + anisotropic filtering ratio-trilinear (GPU-standard MaxAnisotropy=8, minor-axis sets LOD e major/minor sets sample count lungo l'asse maggiore — Heckbert 1989 EWA semplificato, identico a quello che fanno tutti i GPU shipping e Arnold/Cycles "AnisoFilter"). **`FloatTexture` esteso** con overload 5-arg footprint-aware (riduce con channel average lo stesso modo della versione 4-arg). **Materiali rifattorizzati**: bulk-rewrite degli `Albedo.Value(rec.U, rec.V, rec.LocalPoint, rec.ObjectSeed)` → `Albedo.Value(..., rec.Footprint)` su `Lambertian`, `Dielectric`, `Metal`, `DisneyBsdf` (BaseColor + SubsurfaceColor) → 19 callsites totali, ognuno passa il footprint correttamente quando disponibile. **Nuovo CLI flag** `--texture-filtering <auto|on|off>` (default `auto` = on; `off` disabilita l'emissione di differentials per benchmark A/B contro la baseline point-sampled). **Nuovi unit test** in `RayDifferentialTests` (9 test, 5 fact + 4 theory rows): footprint cresce linearmente con la distanza di hit, footprint vuoto quando ray non porta differentials, Transform scala il footprint con la Jacobiana inversa, `NoiseTexture` pass-through bit-identica senza footprint, `NoiseTexture` riduce la varianza con wide footprint. Tutti i 252 test passano (243 pre-esistenti + 9 nuovi). Back-compat byte-identica: omettendo `--texture-filtering` o forzandolo a `auto` su ogni scena che usa solo texture point-sampled, l'output è identico al pre-cambio (il default overload `ITexture.Value(...,footprint)` è pass-through alla versione 4-arg). **File toccati**: `Core/{Ray, HitRecord, RayDifferential, FilterFootprint, FootprintMath}.cs` (new/extended), `Camera/Camera.cs`, `Geometry/{Transform, Sphere, Quad, Triangle}.cs`, `Textures/{ITexture, FloatTexture, NoiseTexture, VoronoiTexture, ImageTexture}.cs`, `Materials/{Lambertian, Dielectric, Metal, DisneyBsdf}.cs`, `Rendering/Renderer.cs`, `Program.cs`. **Step rimanenti del TODO genitore** (2-7: color ramp multi-stop, smooth Voronoi, Musgrave HeteroTerrain/HybridMultifractal, Marble/Wood studio-quality, F3/F4 Voronoi outputs, CoordinateTexture node): cicli successivi.

- **2026-05 — Bump + displacement combination / autobump (step 5/5 surface displacement stack).** Branch `claude/bump-displacement-combo-lnMhu`. Quinto e ultimo step della roadmap "Stack completo surface displacement livello Arnold/RenderMan/Cycles": il displacement (scalare o vector) e il bump map a livello materiale possono ora coesistere correttamente sulla stessa entità, e il loader sa derivare un *autobump* — la bump map residua dalla stessa texture di displacement che recupera la coda alta-frequenza sotto la risoluzione della griglia di subdivision. Allineamento puntuale ad Arnold (`autobump_visibility` su `polymesh`), RenderMan (workflow `PxrDisplace` + `PxrBump` sulla stessa texture) e Cycles (Displacement node con output "Both"). **Composizione canonica** finalmente esplicitata e implementata in `Renderer.ShadeSurface`: `geometry normal (post-displacement) → material.normal_map → material.bump_map → mesh.autobump`. Il `coat_normal_map` Disney resta indipendente per design (parità con Arnold standard surface e Cycles Principled BSDF). **Nuovi YAML field** sulla `DisplacementData`: `autobump: bool` (default `false`, back-compat byte-identica), `autobump_strength: float` (default `1`, ampiezza finale `autobump_strength · |scale|` — la scelta di legare le due all'amplitudine del displacement è la stessa di Arnold) e `autobump_scale: float` (moltiplicatore di frequenza UV stackato su `displacement.uv_scale`, default `1`). **Nuovi campi engine**: `DisplacementOptions.{Autobump, AutobumpStrength, AutobumpScale, IsAutobumpActive}`, `HitRecord.AutoBump` (slot indipendente dal materiale), `Mesh.AutoBump` (per-mesh, propagato a `rec.AutoBump` su hit). **Decisione architetturale chiave**: l'autobump vive sulla mesh, non sul materiale, così due mesh che condividono lo stesso `IMaterial` possono opt-in indipendentemente — preservando la condivisione dei materiali nel resto della scena (in Arnold l'autobump è anch'esso un flag per-shape). `ObjLoader.Load` costruisce la `BumpMapTexture` quando `displacement.IsAutobumpActive` e la attacca al `Mesh` prima di restituirlo. `SceneLoader.BuildDisplacementOptions` parsa i tre nuovi field, emette warning su `autobump_strength ≤ 0`, e la line "Mesh:" stampa il tag `scalar+autobump` / `vector-tangent+autobump` per visibilità. **Showcase nuovo** `scenes/showcases/bump-displacement-combo-showcase.yaml`: quattro pannelli verticali CC×4 fianco a fianco (A flat reference / B solo displacement / C displacement + autobump / D displacement + autobump + bump materiale "graffi") sotto key direzionale radente + scoglio vector-tangent + autobump sopra al centro — confronto diretto del recupero dettaglio. **Nuovi unit test** in `BumpDisplacementCombinationTests` (8): `IsAutobumpActive` contract (flag + texture + strength), propagation `Mesh.AutoBump → HitRecord.AutoBump` su hit, null-propagation senza autobump, end-to-end `ObjLoader.Load` autobump on/off, scaling lineare di `autobump_strength` (raddoppia la tangente del tilt), indipendenza `mesh.AutoBump` da `material.BumpMap` (compongono, non si escludono), back-compat byte-identica su 3-arg/9-arg overload con autobump disattivato. Doc: `scene-reference.md` §7.12 sezione "Autobump", `riferimento-scene.md` §7.12 sezione "Autobump", tutorial EN/IT §4.12.4. **README.md** in root aggiornato con sezione dedicata al surface displacement stack (steps 1-5). Back-compat: 243/243 test (235 pre-esistenti + 8 nuovi), omettendo `autobump` (o con flag off) ogni scena step-1/2/3/4 rende byte-identica. **Conclusione del ciclo "Stack completo surface displacement"**: tutti e cinque gli step (bump map → subdivision → scalar displacement → vector displacement → autobump+composizione) completati con parità feature ai render pro.

- **2026-05 — Vector displacement RGB→XYZ (step 4/5 surface displacement stack).** Branch `claude/vector-displacement-step4-nPhcY`. Quarto step della roadmap "Stack completo surface displacement livello Arnold/RenderMan/Cycles": l'engine di displacement esistente è stato esteso per leggere il triplet RGB della texture come offset 3D, sbloccando overhang, crinkles e qualunque feature che si pieghi su se stessa — feature che un height-field scalare puro non può rappresentare per costruzione. Allineamento puntuale ad Arnold (`vector_displacement` con `vector_space: tangent|object`), Cycles ("vector displacement" + "tangent vector displacement") e RenderMan (`PxrDispVectorLayer`). Formula: `v' = v + scale · (rgb − midlevel) · basis`. In tangent space la convenzione è R→T, G→B (bitangente), B→N (normale): tangente e bitangente per-vertex sono ricavate via formula Lengyel 2001 dal gradiente UV sulla topologia di displacement (post-subdivision, pre-triangulation), accumulate angle-weighted attraverso i triangoli incidenti, ortonormalizzate via Gram-Schmidt contro la normale smooth e con handedness preservata dalla bitangente accumulata (regola MikkTSpace). In object space il triplet RGB è sommato direttamente alla posizione locale, senza rotazione TBN — utile per sculpt condivisi tra asset con UV diversi. Vettori senza UV in tangent space cadono silenziosamente in object space (stesso comportamento di Arnold), riportando un warning solo dal loader YAML. **Nuovo enum** `DisplacementMode { Scalar, Vector }` e `DisplacementSpace { Tangent, Object }` su `DisplacementOptions`; `DisplacementEngine.Apply` dispatcha sul mode, riutilizza il calcolo delle normali smooth e (in vector tangent) aggiunge un singolo passo di costruzione del TBN per-vertex. **Reporting esteso** della max-displacement come lunghezza Euclidea dell'offset (non più ampiezza scalare) — questo è ciò che `displacement_bound` deve enclosare. **Nuovo default bound** per vector mode: `|scale|·√3` (limite L2 di un vettore le cui tre componenti possono ciascuna toccare `|scale|`), contro `|scale|` per scalar; il warning di under-bounding indica il valore corretto. **YAML field nuovi** sulla `DisplacementData`: `mode: scalar|vector` (default `scalar`, back-compat per tutte le scene step-3), `space: tangent|object` (default `tangent`, ignorato in scalar con warning); `BuildDisplacementOptions` valida entrambi i campi e produce warnings strutturati per stringhe sconosciute. **Showcase nuovo** `scenes/showcases/vector-displacement-showcase.yaml`: tre pannelli verticali CC×6 con lo stesso noise fBm (A scalar reference, B vector tangent-space, C vector object-space) + asteroide CC×4 con ridged-fBm vector tangent — confronto silhouette diretto contro `scalar-displacement-showcase.yaml`. **Nuovi unit test** in `DisplacementTests` (8): channel mapping R→T/G→B/B→N esatto su quad piatto con TBN nota, object-space treats RGB as raw XYZ, midlevel-equal-rgb produce zero offset, no-UV fallback a object space, maxDisplacement è la lunghezza Euclidea, pure-N tangent vector ≡ scalar height-field (sanity-check di equivalenza tra modi), disabled è no-op, displaced quasi-sphere AABB cresce in ogni direzione con offset radiale. Doc: `scene-reference.md` §7.12 ("Vector displacement"), `riferimento-scene.md` §7.12, tutorial EN/IT §4.12.3. Back-compat: 235/235 test (227 pre-esistenti + 8 nuovi), omettendo `mode` o impostandolo a `"scalar"` ogni scena step-3 rende byte-identica. **Step rimanente del TODO genitore (5: autobump-like combination bump + displacement con priorità di applicazione coerente con Disney BSDF): ciclo successivo.**

- **2026-05 — Scalar displacement vero (step 3/5 surface displacement stack).** Branch `claude/scalar-displacement-step3-jBsKA`. Terzo step della roadmap "Stack completo surface displacement livello Arnold/RenderMan/Cycles": il loader OBJ applica un displacement scalare ai vertici della mesh (sub)divisa prima della costruzione del BVH, producendo silhouette reali (non solo perturbazione di shading come il `bump_map` dello step 1). Formula canonica `v' = v + scale · (h − midlevel) · n_smooth` con `h = luminance(texture.Value(u, v, p))` Rec.709 e `n_smooth` media pesata sugli angoli delle normali di faccia incidenti sulla topologia limite (Max 1999 — default Blender/Maya/OpenSubdiv). Allineamento puntuale ad Arnold (`displacementShader`, `disp_padding`), RenderMan (`PxrDisplace`, `dispMidpoint`/`dispBound`) e Cycles ("True Displacement"). Nuovi file: `Geometry/Subdivision/{DisplacementOptions, DisplacementEngine}.cs`, `Geometry/BoundsInflatedHittable.cs` (wrapper che pad gli AABB delle foglie del BVH per il `displacement_bound`). Pipeline integrata in `ObjLoader.Load`: `parse OBJ → PolyMesh → optional subdivision (Loop/CC) → optional displacement → triangulate (con ricalcolo normali post-displacement) → Mesh+BVH`. Lookup UV per-vertex: `BuildVertexUvLookup` sceglie il primo corner incidente per vertice (convenzione vertex-varying di OpenSubdiv); le procedurali 3D (noise/marble/wood/voronoi) campionano direttamente sulla posizione e restano seam-free anche su mesh senza UV. Mesh constructor esteso con `leafBoundsInflation` opzionale che wrappa ogni triangolo in un `BoundsInflatedHittable` prima del BVH build — i path no-displacement restano identici bit-for-bit. Nuovi YAML field su `EntityData` mesh: blocco `displacement: { texture, scale, midlevel, uv_scale }` e scalare `displacement_bound`; bound default `|scale|` quando omesso, warning quando il displacement effettivamente applicato lo supera (sanity check forward-compat per future modalità lazy). SceneLoader: `BuildDisplacementOptions` riusa `CreateTexture()` per qualunque ITexture (procedurali + image); seed dell'entity propagato al sampling delle procedurali (instance variation). Reporting esteso nella line "Mesh:" con `displacement: max=…`. Vincolo architetturale `mesh-only` (matches Arnold/Cycles): le primitive built-in continuano a usare solo `bump_map`. Nuovo modello `scenes/models/displacement-plane.obj` (quad unitario in XZ, +Y winding, con UV corner). Showcase `scenes/showcases/scalar-displacement-showcase.yaml`: tre pannelli verticali CC×6 (flat reference / Perlin fBm / Voronoi F2-F1) + asteroide CC×4 con ridged-fBm, sotto key direzionale + fill point. 12 unit test nuovi in `DisplacementTests` (no-op su disabled, uniform-height shift along smooth normal, midlevel-equal-h è zero, scala negativa è inward, max-disp reporting, vertex UV lookup picks first corner, no-UV fallback su posizione, uv_scale shifts lookup, mesh vuota safe, displaced-cube BBox cresce in ogni direzione). Doc: `scene-reference.md` §7.12 (sezione "Scalar displacement"), `riferimento-scene.md` §7.12, tutorial EN/IT §4.12.2. Back-compat: 227/227 test (215 pre-esistenti + 12 nuovi), omettendo il blocco `displacement` le scene rendono identiche. **Step rimanenti del TODO genitore (4-5: vector displacement RGB→XYZ, autobump-like combination bump+displacement): cicli successivi.**

- **2026-05 — Mesh subdivision Loop + Catmull-Clark (step 2/5 surface displacement stack).** Branch `claude/mesh-subdivision-algorithms-xN4q0`. Secondo step della roadmap "Stack completo surface displacement livello Arnold/RenderMan/Cycles": il loader OBJ può ora raffinare la mesh in input usando i due algoritmi production-grade canonici di OpenSubdiv/Arnold/RenderMan/Cycles. **Loop** (Charles Loop 1987 — tesi MSc Utah) per mesh triangolari, con maschere di bordo Hoppe et al. 1994 (cubic B-spline 6/8/1/8/1/8 sul curve aperto, corner retained su valenza ≠2 al bordo) e mask interno classico `(1 − n·β)·v + β·Σ vᵢ` con `β = (1/n)(5/8 − (3/8 + ¼·cos(2π/n))²)`. **Catmull-Clark** (1978) per mesh quad (e mixed-arity nel primo passo), con face point / edge point interno (4-punti) e di bordo (mid-edge) / vertex point interno `(Q + 2R + (n−3)S)/n` e di bordo Hoppe. Driver `SubdivisionEngine` con risoluzione `Auto` (CC per quad puro, Loop per tri puro, CC per mixed) e adaptive screen-space `subdivision_pixel_error` che proietta l'edge più lungo della mesh-in-world-space tramite la camera risolta e sceglie il numero minimo di iterazioni per portarlo sotto la soglia (ceil(log₂(longestPx/target))), capped da `subdivision_max_iterations` (default 6 → 4 096× facce). Nuovi file: `Geometry/Subdivision/{PolyMesh, SubdivisionOptions, LoopSubdivider, CatmullClarkSubdivider, SubdivisionEngine}.cs`. `ObjLoader` rifattorizzato: ora costruisce sempre prima un `PolyMesh` (preservando l'arity per CC), poi opzionalmente subdivide e poi triangola; il path "no-subdivision" replica bit-for-bit il comportamento storico. Normali di shading sempre **ricalcolate dalla topologia limite** con la media pesata sugli angoli (Max 1999 — default di Blender/Maya); i normali sorgente vengono propagati attraverso le iterazioni come canale vertex-varying ma sovrascritti alla triangolazione finale (la limit surface è più liscia dei normali interpolati). UV vertex-varying con maschere lineari sui midpoint, cuciture UV (stesso position ma UV diversi su facce adiacenti) preservate. Nuovi YAML field sulla `EntityData` mesh: `subdivision_scheme: loop|catmull_clark|auto|none`, `subdivision_iterations` (uniforme), `subdivision_pixel_error` (adattivo), `subdivision_max_iterations`. Showcase `scenes/showcases/mesh-subdivision-showcase.yaml`: due row (cubi CC 0/1/2/3 + icosa Loop 0/1/2/3) + sphere centrale adaptive a 6 px target che sceglie autonomamente Loop×3. Modelli OBJ low-poly inline: `scenes/models/subdivision-cube.obj` (6 quad), `scenes/models/subdivision-icosahedron.obj` (20 tri sulla sfera unitaria). 19 unit test nuovi in `SubdivisionTests` (face-count ×4, planar-stays-planar, boundary mask analitica su triangolo aperto, interior mask sul quadrato piano coplanare, CC contract-toward-sphere, simmetria ottaedrica preservata, UV carried-through, Auto scheme picks correttamente CC/Loop, MaxIterations clamp, adaptive 0-iter quando lontano / ≥1 quando vicino, end-to-end Triangulate emette `SmoothTriangle` con normali unitarie). Performance: 768 facce (CC×3) o 1 280 facce (Loop×3) renderizzano in <1s a 320×200, 4 spp. Doc: `scene-reference.md` §7.12.1, `riferimento-scene.md` §7.12 (sezione subdivision), tutorial EN/IT §4.12.1. Back-compat: 215/215 test (196 pre-esistenti + 19 nuovi), omettendo i nuovi field le mesh rendono identiche. **Step rimanenti del TODO genitore (3-5: scalar displacement vero con `displacement_bound` AABB inflation, vector displacement, autobump-like combination bump+displacement): cicli successivi.**

- **2026-05 — Bump map (step 1/5 surface displacement stack).** Branch `claude/surface-displacement-implementation-Gyg0x`. Primo step della roadmap "Stack completo surface displacement livello Arnold/RenderMan/Cycles": nuovo canale `bump_map: { texture, strength, scale }` su tutti i materiali (Lambertian, Metal, Dielectric, Emissive, DisneyBsdf, MixMaterial). La normale di shading è perturbata in tangent space via differenze centrate (Blinn 1978, du=dv=1e-3) della luminanza Rec.709 della texture interna; ogni sample perturba sia (u,v) sia il punto 3D `p` lungo T/B, così sia le texture procedurali 3D (noise/marble/wood/voronoi) sia le texture image 2D producono un gradiente coerente. Compone con `normal_map` (applicato prima) con re-ortogonalizzazione Gram-Schmidt della TBN contro la N già perturbata (convenzione Arnold/Cycles); il `coat_normal_map` Disney resta indipendente per design. Funziona su tutte le primitive (Sphere, Box, Cylinder, Cone, Quad, Disk, Annulus, Torus, Capsule, Lathe, Triangle, SmoothTriangle, InfinitePlane — tutte popolano `rec.Tangent`/`rec.Bitangent`). Nuovo `Textures/BumpMapTexture` (clamp strength [0,10], scale>0); nuovo `BumpMapData` YAML + dispatcher `LoadBumpMap` in `SceneLoader` che riusa `CreateTexture()`; hook `ApplyBumpMap` in `Renderer.ShadeSurface` subito dopo `ApplyNormalMap`. Showcase `scenes/showcases/bump-map-showcase.yaml` con 4 quad fianco a fianco (flat reference / noise fBm / marble veins / image concrete) sotto luce point radente. 9 unit test in `BumpMapTests` (luminance reduction, gradient direzione, clamp strength/scale, scale-shifts-sample-point). Doc: `scene-reference.md` §6, `riferimento-scene.md` §6, tutorial §3.11 (EN+IT). Back-compat: 196/196 test, omettendo `bump_map` le scene rendono identiche. **Step rimanenti del TODO genitore (2-5: subdivision, scalar displacement, vector displacement, autobump combination): cicli successivi.**


Sintesi cronologica dei cicli grossi. Per i dettagli matematici e i riferimenti vedi i doc tecnici puntati.

- **2026-05 — Pro-grade procedural textures.** Branch `claude/implement-devlog-todo-nz1Ps`. Allineamento di noise/marble/wood ad Arnold (`noise`, `marble`), Cycles (Noise/Voronoi/Brick/Gradient) e RenderMan (`PxrFractal`, `PxrVoronoise`, `PxrMarble`, `PxrTile`). Engine: `Perlin.Fbm/Ridged/Billow/NoiseVector` + nuovo `Core/WorleyNoise` (F1/F2 con metriche euclidean/manhattan/chebyshev + cell-id hash). `NoiseTexture` ora con `noise_type` (perlin/fbm/turbulence/ridged/billow), `octaves`, `lacunarity`, `gain`, `distortion`; `MarbleTexture` con `vein_axis`, `vein_frequency`, `vein_sharpness` (Carrara look ≥4), `noise_type` modulator, `distortion`; `WoodTexture` con `ring_axis`, `ring_sharpness` (triangular-wave sharpening), `axial_grain`, `octaves` fBm, `distortion`. Nuove `VoronoiTexture` (output f1/f2/f2_minus_f1/f1_plus_f2/cell), `BrickTexture` (running-bond + per-brick color variation + weathering noise), `GradientTexture` (linear/quadratic/easing/spherical/radial). Backward-compat preservato: omettendo i nuovi parametri le scene legacy renderizzano identiche (NoiseStrength=0 → Perlin, NoiseStrength>0 → 7-octave Turbulence; marble VeinSharpness=1 → output legacy; wood Octaves=1 → single-Perlin legacy). SceneLoader: extension a `TextureData` (24 nuovi campi), dispatch su alias `voronoi|worley|cell|cellular`, `brick|bricks|tile`, `gradient|ramp`. Showcase `scenes/showcases/textures-pro-showcase.yaml` (3×3 sfere su pavimento marmo). Doc: `scene-reference.md` §6, `riferimento-scene.md` §6, tutorial §3.8 (EN+IT). 187/187 test passano.

- **2026-05 — `focal_pos` (camera focus point).** Branch `claude/implement-renderer-integration-QsPuK`. Allineamento ad Arnold "Focus Object", Cycles "Focal Object" e RenderMan: la camera può specificare un punto 3D `focal_pos: [x, y, z]` invece dello scalare `focal_dist`. Il loader (`SceneLoader.ComputeFocusDistance`) calcola la distanza come **proiezione** del vettore camera→focal-point sull'asse ottico (`(F − P) · normalize(lookAt − P)`) — quindi un punto off-axis a `(3, 4, −5)` con camera all'origine guardando `−Z` produce focus distance `5`, non `√50`. Precedenza: `focal_pos` vince su `focal_dist`; warning + fallback su input degeneri (punto dietro la camera, camera con `lookAt == position`, lista YAML malformata). `Camera` invariata (single-source-of-truth sullo scalare `focusDist`). Showcase `scenes/showcases/focal-pos-showcase.yaml` con 4 camere (near/middle/far + sanity A/B equiv via `focal_dist`). 9 unit test in `CameraFocalPosTests`. Doc: scene-reference §4, tutorial §7.5 (EN+IT).

- **2026-05 — `visible_to_camera` (camera visibility flag).** Branch `claude/implement-renderer-integration-QsPuK`. Allineamento ad Arnold (`camera` visibility) e Cycles ("Ray Visibility → Camera"): proxy di `sphere`/`area` light e qualunque entity (utile su emissive panels) possono essere nascosti dai raggi primari mantenendo NEE, riflessioni speculari, rifrazioni e indirect attivi. Implementazione: nuovo `Geometry/CameraInvisibleHittable` (modello `BackFaceCulledHittable`), nuovo flag `HitRecord.CameraInvisible`, skip-loop in `Renderer.TraceRay` con cap di 8 attivo solo su `depth == _maxDepth` (chiusura MIS di Veach preservata: proxy resta nel BVH per i non-primari). YAML `visible_to_camera: bool = true` su `LightData` ed `EntityData`. Showcase `scenes/showcases/visible-to-camera-showcase.yaml`, regression `CameraVisibilityTests` (4 test). Doc: scene-reference §7 (campi comuni entity), §8.4/§8.5.

- **2026-05 — Transparent shadow rays.** Branch `claude/fix-transparent-shadows-4n8Ph` (PR #50). Strada 1 della roadmap caustiche. Lo shadow ray attraversa superfici trasmissive con `(1 − F) · tint` per interfaccia + `exp(−σ_a · d)` Beer-Lambert sul segmento interno; nuovo `Geometry/ShadowRay.Transmittance`, override `IMaterial.{ShadowTransmittance, ShadowAbsorption}` su `Dielectric` / `DisneyBsdf` (entrambi quando `spec_trans > 0`) / `MixMaterial`; tutti gli 8 light dispatcher aggiornati. Vetri colorati con `transmission_depth > 0` (rubino, smeraldo, ambra, zaffiro) ora proiettano ombra tinta. Doc: `docs/technical/path-tracing-and-lighting.md` §2.3.

- **2026-05 — Extrusion primitive.** Nuovo `Extrusion : IHittable, ISamplable` (linear / catmull_rom / bezier, twist + taper, caps configurabili, BVH interno) + utility 2D `Polygon2D` (signed area, ear clipping, tessellation chiusa). Doc: scene-reference §7.17. 9 test nuovi (155 totali).

- **2026-05 — World/Sky cleanup.** Branch `claude/review-ambient-light-sky-vX2YO`. Rimosso il termine `world.ambient_light` non-fisico (sommato come radianza grezza in `ComputeDirectLighting`, fuori da BRDF/coseno). Sostituito `world.background` con `world.sky.type: flat`. Aggiunto NEE flat-sky uniforme (`pdf = 1/(4π)`) allineato a Cycles/Arnold. Test guard-rail `BlackLambertianFloor_RendersBlack_WithBlackSky`. Breaking YAML.

- **2026-04 — Light hardening.** Branch firefly-killer su `Lights/`. `SoftRadius` su Area/Sphere/Geometry (clamp `distSq = max(., r²)`). Depth-aware indirect clamp (`--indirect-clamp-factor`). LightDistribution power-CDF (`--light-sampling`). Sun disc su DirectionalLight (`angular_radius`). ShadowSamples + softRadius su SpotLight. `ISamplable.SurfaceArea` deterministico → niente PRNG nel costruttore di GeometryLight (scene-load 100% deterministico). 5 test regressione firefly nuovi.

- **2026-04 — MIS completion.** Branch `claude/complete-mis-implementation-q98Tb`. MIS esteso a tutti i materiali (Lambertian, Metal, MixMaterial, oltre a Disney) e alla phase function volumetrica. CLI `--mis <balance|power>`. Test `MisMaterialsTests` (140/140). Doc: `multiple-importance-sampling.md`.

- **2026-04 — Disney BSDF & sampling review.** Branch `claude/review-disney-materials-QO2mO`. Tutti i parametri Disney mancanti (`anisotropic`, `transmission_color`/`depth`, `thin_walled`, `diff_trans`, `flatness`, `subsurface_color`, `coat_*`, `sheen_roughness`, `thin_film_*`). Kulla-Conty LUT, VNDF (Heitz 2018), Charlie sheen (Estevez-Kulla 2017), thin-film (Belcour-Barla 2017), Sobol+Owen sampler. Cleanup `IMaterial` (rimossi campi Blinn-Phong, sostituiti da `NeedsDirectLighting` / `IsDeltaScatter`).

- **2026-04 — Scene classification refactor.** Branch `claude/review-raytracer-rendering-7hQpu`. Sostituito `ILight.Illuminate(Vector3.Zero)` con `ApproximatePower(AABB)` deterministico, receiver-independent. Finite-scene bounds helper (filtra `InfinitePlane` ±1e6 che inflava R a ~1700). Threshold ricalibrata 1.0 → 0.5. Doc: `rendering-pipeline.md` §2.1.

---

## 🧪 Checklist verifiche

Da eseguire prima di un commit importante.

- [ ] **Smoke**: render `primitive-showcase.yaml` (16 spp), no crash.
- [ ] **Visual regression**: confronto `cornell-box.yaml` con baseline.
- [ ] **Performance**: tempo render scena standard non +5% senza motivo.
- [ ] **YAML**: ogni nuova proprietà ha default sensato.
- [ ] **CSG**: render `csg-showcase.yaml` — union/intersection/subtraction visivamente corrette.
- [ ] **HDRI**: render `hdri-showcase.yaml` — riflessi/rifrazioni/GI corrette.
- [ ] **Mix**: render `mix-material-showcase.yaml` — blend costante (3 livelli), maschere (noise/marble/wood/checker), lava emissiva.
- [ ] **Group**: render `group-showcase.yaml` — transform ereditate, template/istanze, import.
- [ ] **Torus**: render `torus-showcase.yaml` con camera `pinhole`/`dof_soft`/`dof_extreme` — no contorni fantasma, occlusione torus/cone e torus/cylinder corretta.
- [ ] **Import**: materiali e template importati da file esterni funzionano.
- [ ] **Template override**: il materiale dell'istanza sovrascrive quello del template.
- [ ] **Transparent shadows**: render `cornell-box-spheres.yaml` — la sfera di vetro centrale proietta un alone Fresnel-tinted, non un'ombra dura.
